# Strict by default — surface phantom dependencies before they reach consumers.
# Do not enable hoisting workarounds; if a package needs a dep, it must declare it.
strict-peer-dependencies=false
auto-install-peers=true

# Scoped public packages publish to the public registry.
@nodeve:registry=https://registry.npmjs.org/
