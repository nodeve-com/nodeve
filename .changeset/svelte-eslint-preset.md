---
'@nodeve/config': minor
---

Add `@nodeve/config/eslint/svelte`: a SvelteKit-app ESLint factory (svelte recommended + prettier compat + the type-aware `.svelte` parser block) for uniform Svelte linting across nodeve sister repos. Compose it after the base/index config; storybook stays app-side.
