---
'@nodeve/grimoire': minor
---

CLI concept + schema queries. New `feature`/`archetype`/`property`/`part` commands list or dump concept nodes with their inline prose; `schema <kind> <slug> [camel]` dumps the JSON Schema twin. `help`/`-h`/`--help` prints usage to stdout, exit 0. `docs/` ships in the package.
