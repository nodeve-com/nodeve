# This subtree holds Equation air conditioners. Declares the archetype every model here validates
# against (the "parent defines what its children contain" rule); models fill the atoms.
archetype: air_conditioner
