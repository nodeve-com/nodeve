# This subtree holds Emelson dehumidifiers. Declares the archetype every model here validates
# against (the "parent defines what its children contain" rule); models fill the atoms.
archetype_id: dehumidifier
