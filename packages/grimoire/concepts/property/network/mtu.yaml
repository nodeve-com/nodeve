title: { en: MTU }
description: { en: "Maximum transmission unit — the largest L3 payload (bytes) the interface carries in one frame. 1500 is standard Ethernet; up to ~9000 for jumbo frames; 68 is the IPv4 floor." }
schema: { type: integer, minimum: 68, maximum: 65535 }
