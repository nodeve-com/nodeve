title: { en: Get command }
description: { en: "Command prefix that requests a parameter's current value (M4-ATX: [0xa4, 0xa1]); index/length bytes overlaid at their offsets, rest zero-padded to request_length." }
schema: { type: array, minItems: 1, items: { type: integer, minimum: 0, maximum: 255 } }
