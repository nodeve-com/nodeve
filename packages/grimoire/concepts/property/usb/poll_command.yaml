title: { en: Poll command }
description: { en: "Request bytes written to fetch one diagnostics frame (M4-ATX: [0x81, 0x00])." }
schema: { type: array, minItems: 1, items: { type: integer, minimum: 0, maximum: 255 } }
