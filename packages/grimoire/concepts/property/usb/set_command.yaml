title: { en: Set command }
description: { en: "Command prefix that writes a parameter (M4-ATX: [0xa4, 0xa0]); index/length/value overlaid. The device echoes index+value for the writer to verify." }
schema: { type: array, minItems: 1, items: { type: integer, minimum: 0, maximum: 255 } }
