title: { en: Register addresses }
description:
  en: 'Addresses read by a flags decode; labels[i] is the bit map for addresses[i], parallel.'
schema:
  type: array
  minItems: 1
  items: { type: integer, minimum: 0, maximum: 65535 }
