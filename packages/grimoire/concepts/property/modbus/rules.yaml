title: { en: Priority rules }
description: { en: "Evaluated in order: first rule whose (register at address & mask) is non-zero wins; its emit string is published. Each rule = { address, mask, emit }." }
schema: { type: array, minItems: 1, items: { type: object } }
