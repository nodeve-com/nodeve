// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const AddressesSchema: TSchema = Type.Array(Type.Integer({"maximum":65535,"minimum":0}), {"minItems":1});

export type Addresses = Array<number>;

export type AddressesDataT = { readonly "description": { readonly "en": "Addresses read by a flags decode; labels[i] is the bit map for addresses[i], parallel." }; readonly "schema": { readonly "items": { readonly "maximum": 65535; readonly "minimum": 0; readonly "type": "integer" }; readonly "minItems": 1; readonly "type": "array" }; readonly "title": { readonly "en": "Register addresses" } };

const _data: AddressesDataT = {
	"description": {
		"en": "Addresses read by a flags decode; labels[i] is the bit map for addresses[i], parallel."
	},
	"schema": {
		"items": {
			"maximum": 65535,
			"minimum": 0,
			"type": "integer"
		},
		"minItems": 1,
		"type": "array"
	},
	"title": {
		"en": "Register addresses"
	}
};
export default _data;
