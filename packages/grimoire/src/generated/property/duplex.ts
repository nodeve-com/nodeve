// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const DuplexSchema: TSchema = Type.Union([Type.Literal("half"), Type.Literal("full")], {"type":"string"});

export type Duplex = "half" | "full";

export type DuplexDataT = { readonly "description": { readonly "en": "Duplex mode of the link: full transmits and receives simultaneously; half shares one channel (CSMA/CD collision domain)." }; readonly "schema": { readonly "enum": readonly ["half", "full"]; readonly "type": "string" }; readonly "title": { readonly "en": "Duplex" } };

const _data: DuplexDataT = {
	"description": {
		"en": "Duplex mode of the link: full transmits and receives simultaneously; half shares one channel (CSMA/CD collision domain)."
	},
	"schema": {
		"enum": [
			"half",
			"full"
		],
		"type": "string"
	},
	"title": {
		"en": "Duplex"
	}
};
export default _data;
