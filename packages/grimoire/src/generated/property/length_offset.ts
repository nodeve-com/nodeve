// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const LengthOffsetSchema: TSchema = Type.Integer({"minimum":0});

export type LengthOffset = number;

export type LengthOffsetDataT = { readonly "description": { readonly "en": "Where the parameter's byte-length sits in the command (M4-ATX: 3) — the device echoes the width it should read/write." }; readonly "schema": { readonly "minimum": 0; readonly "type": "integer" }; readonly "title": { readonly "en": "Length offset" } };

const _data: LengthOffsetDataT = {
	"description": {
		"en": "Where the parameter's byte-length sits in the command (M4-ATX: 3) — the device echoes the width it should read/write."
	},
	"schema": {
		"minimum": 0,
		"type": "integer"
	},
	"title": {
		"en": "Length offset"
	}
};
export default _data;
