// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const ByteOrderSchema: TSchema = Type.Union([Type.Literal("big_first"), Type.Literal("little_first")], {"type":"string"});

export type ByteOrder = "big_first" | "little_first";

export type ByteOrderDataT = { readonly "description": { readonly "en": "Byte order for multi-byte types; omit ⇒ big_first (the network/HID default). Ignored for single-byte types." }; readonly "schema": { readonly "enum": readonly ["big_first", "little_first"]; readonly "type": "string" }; readonly "title": { readonly "en": "Byte order"; readonly "pt": "Ordem de bytes" } };

const _data: ByteOrderDataT = {
	"description": {
		"en": "Byte order for multi-byte types; omit ⇒ big_first (the network/HID default). Ignored for single-byte types."
	},
	"schema": {
		"enum": [
			"big_first",
			"little_first"
		],
		"type": "string"
	},
	"title": {
		"en": "Byte order",
		"pt": "Ordem de bytes"
	}
};
export default _data;
