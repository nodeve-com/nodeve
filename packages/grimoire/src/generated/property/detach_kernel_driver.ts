// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const DetachKernelDriverSchema: TSchema = Type.Boolean();

export type DetachKernelDriver = boolean;

export type DetachKernelDriverDataT = { readonly "description": { readonly "en": "Linux fact that the in-kernel usbhid driver usually owns the device, so a userspace reader must detach it first (m4api does exactly this)." }; readonly "schema": { readonly "type": "boolean" }; readonly "title": { readonly "en": "Detach kernel driver" } };

const _data: DetachKernelDriverDataT = {
	"description": {
		"en": "Linux fact that the in-kernel usbhid driver usually owns the device, so a userspace reader must detach it first (m4api does exactly this)."
	},
	"schema": {
		"type": "boolean"
	},
	"title": {
		"en": "Detach kernel driver"
	}
};
export default _data;
