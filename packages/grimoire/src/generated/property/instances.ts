// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const InstancesSchema: TSchema = Type.Array(Type.Object({}));

export type Instances = Array<Record<string, never>>;

export type InstancesDataT = { readonly "description": { readonly "en": "Sparse per-instance overrides, each tagged with its phase leg/pair (the join key); only instances that DIFFER from default appear, deep-merged over it (UPDATE ... WHERE phase=ab)." }; readonly "title": { readonly "en": "Instances" } };

const _data: InstancesDataT = {
	"description": {
		"en": "Sparse per-instance overrides, each tagged with its phase leg/pair (the join key); only instances that DIFFER from default appear, deep-merged over it (UPDATE ... WHERE phase=ab)."
	},
	"title": {
		"en": "Instances"
	}
};
export default _data;
