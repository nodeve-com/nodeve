// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const ResolutionSchema: TSchema = Type.Number({"exclusiveMinimum":0});

export type Resolution = number;

export type ResolutionDataT = { readonly "description": { readonly "en": "Quantization step, in the measurement's unit — smallest distinguishable change (VIM 4.14). Omit when the datasheet doesn't state it; never fabricate." }; readonly "refs": readonly [{ readonly "match": "exact"; readonly "registry_id": "vim"; readonly "term": "4.14" }, { readonly "match": "exact"; readonly "registry_id": "ssn_system"; readonly "term": "Resolution" }]; readonly "schema": { readonly "exclusiveMinimum": 0; readonly "type": "number" }; readonly "title": { readonly "en": "Resolution" } };

const _data: ResolutionDataT = {
	"description": {
		"en": "Quantization step, in the measurement's unit — smallest distinguishable change (VIM 4.14). Omit when the datasheet doesn't state it; never fabricate."
	},
	"refs": [
		{
			"match": "exact",
			"registry_id": "vim",
			"term": "4.14"
		},
		{
			"match": "exact",
			"registry_id": "ssn_system",
			"term": "Resolution"
		}
	],
	"schema": {
		"exclusiveMinimum": 0,
		"type": "number"
	},
	"title": {
		"en": "Resolution"
	}
};
export default _data;
