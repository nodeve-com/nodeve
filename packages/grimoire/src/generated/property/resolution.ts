// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const ResolutionSchema: TSchema = Type.Number({"exclusiveMinimum":0});

export type Resolution = number;

export type ResolutionDataT = { readonly "description": { readonly "en": "Quantization step, in the measurement's unit — smallest distinguishable change (VIM 4.14). Omit when the datasheet doesn't state it; never fabricate." }; readonly "refs": readonly [{ readonly "match": "exact"; readonly "registryId": "vim"; readonly "term": "4.14" }, { readonly "match": "exact"; readonly "registryId": "ssn_system"; readonly "term": "Resolution" }]; readonly "title": { readonly "en": "Resolution" } };

const _data: ResolutionDataT = {
	"description": {
		"en": "Quantization step, in the measurement's unit — smallest distinguishable change (VIM 4.14). Omit when the datasheet doesn't state it; never fabricate."
	},
	"refs": [
		{
			"match": "exact",
			"registryId": "vim",
			"term": "4.14"
		},
		{
			"match": "exact",
			"registryId": "ssn_system",
			"term": "Resolution"
		}
	],
	"title": {
		"en": "Resolution"
	}
};
export default _data;
