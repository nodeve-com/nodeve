// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';

export const schema: TSchema = Type.Integer({"minimum":1});

export type ExponentialMovingAverageMs = number;

type DataT = { readonly "description": { readonly "en": "EMA time constant τ, integer milliseconds — smoothing authored as TIME, never α or sample counts. Sample-relative and Δt-DEPENDENT: a realizer pins α = 1 − e^(−Δt/τ) from the source's observed/commanded interval; unknown Δt ⇒ fall back to a wall-clock mean over the same window." }; readonly "identity": { readonly "archetypeId": "property"; readonly "slug": "exponential_moving_average_ms" }; readonly "title": { readonly "en": "Exponential moving average τ (ms)"; readonly "pt": "Média móvel exponencial τ (ms)" } };

const _data: DataT = {
	"description": {
		"en": "EMA time constant τ, integer milliseconds — smoothing authored as TIME, never α or sample counts. Sample-relative and Δt-DEPENDENT: a realizer pins α = 1 − e^(−Δt/τ) from the source's observed/commanded interval; unknown Δt ⇒ fall back to a wall-clock mean over the same window."
	},
	"identity": {
		"archetypeId": "property",
		"slug": "exponential_moving_average_ms"
	},
	"title": {
		"en": "Exponential moving average τ (ms)",
		"pt": "Média móvel exponencial τ (ms)"
	}
};
export const { description, identity, title } = _data;
