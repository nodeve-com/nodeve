// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const BaseUrlSchema: TSchema = Type.String({"minLength":1});

export type BaseUrl = string;

export type BaseUrlDataT = { readonly "description": { readonly "en": "Base URL under which an external database record resolves — a registry's alternative to iri_template."; readonly "pt": "URL base sob a qual um registro de base de dados externo resolve — a alternativa da iri_template de um registro." }; readonly "title": { readonly "en": "Base URL"; readonly "pt": "URL base" } };

const _data: BaseUrlDataT = {
	"description": {
		"en": "Base URL under which an external database record resolves — a registry's alternative to iri_template.",
		"pt": "URL base sob a qual um registro de base de dados externo resolve — a alternativa da iri_template de um registro."
	},
	"title": {
		"en": "Base URL",
		"pt": "URL base"
	}
};
export default _data;
