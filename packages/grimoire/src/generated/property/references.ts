// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const ReferencesSchema: TSchema = Type.String({"minLength":1});

export type References = string;

export type ReferencesDataT = { readonly "description": { readonly "en": "Archetype whose identity.slug this field's value must be — the field's foreign-key target."; readonly "pt": "Arquétipo cujo identity.slug o valor deste campo deve ser — o alvo de chave estrangeira do campo." }; readonly "title": { readonly "en": "References"; readonly "pt": "Referencia" } };

const _data: ReferencesDataT = {
	"description": {
		"en": "Archetype whose identity.slug this field's value must be — the field's foreign-key target.",
		"pt": "Arquétipo cujo identity.slug o valor deste campo deve ser — o alvo de chave estrangeira do campo."
	},
	"title": {
		"en": "References",
		"pt": "Referencia"
	}
};
export default _data;
