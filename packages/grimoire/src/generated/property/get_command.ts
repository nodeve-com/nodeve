// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';

export const schema: TSchema = Type.Array(Type.Integer({"maximum":255,"minimum":0}), {"minItems":1});

export type GetCommand = Array<number>;

type DataT = { readonly "description": { readonly "en": "Command prefix that requests a parameter's current value (M4-ATX: [0xa4, 0xa1]); index/length bytes overlaid at their offsets, rest zero-padded to request_length." }; readonly "identity": { readonly "archetype": "property"; readonly "slug": "get_command" }; readonly "title": { readonly "en": "Get command" } };

const _data: DataT = {
	"description": {
		"en": "Command prefix that requests a parameter's current value (M4-ATX: [0xa4, 0xa1]); index/length bytes overlaid at their offsets, rest zero-padded to request_length."
	},
	"identity": {
		"archetype": "property",
		"slug": "get_command"
	},
	"title": {
		"en": "Get command"
	}
};
export const { description, identity, title } = _data;
