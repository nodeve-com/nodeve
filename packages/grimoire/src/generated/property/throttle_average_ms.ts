// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';

export const schema: TSchema = Type.Integer({"minimum":1});

export type ThrottleAverageMs = number;

type DataT = { readonly "description": { readonly "en": "Mean over a wall-clock window, integer milliseconds — emit the average of all samples seen per window. Frequency-robust: valid whatever the source cadence (unlike a sample-relative EMA)." }; readonly "identity": { readonly "archetypeId": "property"; readonly "slug": "throttle_average_ms" }; readonly "title": { readonly "en": "Throttle average (ms)"; readonly "pt": "Média por janela (ms)" } };

const _data: DataT = {
	"description": {
		"en": "Mean over a wall-clock window, integer milliseconds — emit the average of all samples seen per window. Frequency-robust: valid whatever the source cadence (unlike a sample-relative EMA)."
	},
	"identity": {
		"archetypeId": "property",
		"slug": "throttle_average_ms"
	},
	"title": {
		"en": "Throttle average (ms)",
		"pt": "Média por janela (ms)"
	}
};
export const { description, identity, title } = _data;
