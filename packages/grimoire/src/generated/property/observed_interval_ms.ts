// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const ObservedIntervalMsSchema: TSchema = Type.Integer({"minimum":1});

export type ObservedIntervalMs = number;

export type ObservedIntervalMsDataT = { readonly "description": { readonly "en": "OBSERVED interval between the foreign master's polls, integer ms — measured from real bus activity, never commanded (a passive tap doesn't own the poll clock; distinct from the commanded update_interval_ms). Lets codegen pin sample-relative filters (EMA α = 1 − e^(−Δt/τ)); omitted = unknown, only wall-clock/value-defined filters valid." }; readonly "title": { readonly "en": "Observed interval (ms)"; readonly "pt": "Intervalo observado (ms)" } };

const _data: ObservedIntervalMsDataT = {
	"description": {
		"en": "OBSERVED interval between the foreign master's polls, integer ms — measured from real bus activity, never commanded (a passive tap doesn't own the poll clock; distinct from the commanded update_interval_ms). Lets codegen pin sample-relative filters (EMA α = 1 − e^(−Δt/τ)); omitted = unknown, only wall-clock/value-defined filters valid."
	},
	"title": {
		"en": "Observed interval (ms)",
		"pt": "Intervalo observado (ms)"
	}
};
export default _data;
