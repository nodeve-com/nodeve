// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';

export const schema: TSchema = Type.Number();

export type Value = number;

type DataT = { readonly "description": { readonly "en": "The magnitude of a valued quantity (a qudt:Quantity), in the stated unit — the ONE role-neutral point field of an interval. Alone it is an exact point; with a band (tolerance/margin/min/max) it is the designated centre that band sits around. The role comes from co-present axes: a `zone` operating point (Vmp, Voc), or a rated/designated centre graded `severity: nominal`. (A `threshold`'s switch is its `min`/`max` edges + `trigger_on`, not `value`; a threshold's `value` is only an optional nominal setpoint.)" }; readonly "identity": { readonly "archetypeId": "property"; readonly "slug": "value" }; readonly "refs": readonly [{ readonly "match": "close"; readonly "registryId": "qudt_quantity_kind"; readonly "term": "Quantity" }]; readonly "title": { readonly "en": "Value"; readonly "pt": "Valor" } };

const _data: DataT = {
	"description": {
		"en": "The magnitude of a valued quantity (a qudt:Quantity), in the stated unit — the ONE role-neutral point field of an interval. Alone it is an exact point; with a band (tolerance/margin/min/max) it is the designated centre that band sits around. The role comes from co-present axes: a `zone` operating point (Vmp, Voc), or a rated/designated centre graded `severity: nominal`. (A `threshold`'s switch is its `min`/`max` edges + `trigger_on`, not `value`; a threshold's `value` is only an optional nominal setpoint.)"
	},
	"identity": {
		"archetypeId": "property",
		"slug": "value"
	},
	"refs": [
		{
			"match": "close",
			"registryId": "qudt_quantity_kind",
			"term": "Quantity"
		}
	],
	"title": {
		"en": "Value",
		"pt": "Valor"
	}
};
export const { description, identity, refs, title } = _data;
