// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const DirectionSchema: TSchema = Type.Union([Type.Literal("above"), Type.Literal("below")], {"type":"string"});

export type Direction = "above" | "below";

export type DirectionDataT = { readonly "description": { readonly "en": "Which side of the threshold trips: above ⇒ trips when the reading exceeds `at`; below ⇒ when it drops under. Borrowed from HA numeric_state (above/below)." }; readonly "title": { readonly "en": "Direction"; readonly "pt": "Direção" } };

const _data: DirectionDataT = {
	"description": {
		"en": "Which side of the threshold trips: above ⇒ trips when the reading exceeds `at`; below ⇒ when it drops under. Borrowed from HA numeric_state (above/below)."
	},
	"title": {
		"en": "Direction",
		"pt": "Direção"
	}
};
export default _data;
