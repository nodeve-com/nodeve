// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const DirectionSchema: TSchema = Type.Union([Type.Literal("above"), Type.Literal("below")], {"type":"string"});

export type Direction = "above" | "below";

export type DirectionDataT = { readonly "description": { readonly "en": "Which side of the threshold trips: above ⇒ trips when the reading exceeds `at`; below ⇒ when it drops under. Borrowed from HA numeric_state (above/below)." }; readonly "schema": { readonly "enum": readonly ["above", "below"]; readonly "type": "string" }; readonly "title": { readonly "en": "Direction"; readonly "pt": "Direção" } };

const _data: DirectionDataT = {
	"description": {
		"en": "Which side of the threshold trips: above ⇒ trips when the reading exceeds `at`; below ⇒ when it drops under. Borrowed from HA numeric_state (above/below)."
	},
	"schema": {
		"enum": [
			"above",
			"below"
		],
		"type": "string"
	},
	"title": {
		"en": "Direction",
		"pt": "Direção"
	}
};
export default _data;
