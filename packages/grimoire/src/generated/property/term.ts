// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const TermSchema: TSchema = Type.String({"minLength":1});

export type Term = string;

export type TermDataT = { readonly "description": { readonly "en": "The identifier of an entry WITHIN a registry — the code that, resolved through the registry's IRI template, names the external concept a crosswalk ref points at (e.g. Temperature in qudt_quantity_kind, 2.3 in vim)." }; readonly "title": { readonly "en": "Term"; readonly "pt": "Termo" } };

const _data: TermDataT = {
	"description": {
		"en": "The identifier of an entry WITHIN a registry — the code that, resolved through the registry's IRI template, names the external concept a crosswalk ref points at (e.g. Temperature in qudt_quantity_kind, 2.3 in vim)."
	},
	"title": {
		"en": "Term",
		"pt": "Termo"
	}
};
export default _data;
