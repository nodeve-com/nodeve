// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const NetworkInterfaceIdSchema: TSchema = Type.String({"minLength":1});

export type NetworkInterfaceId = string;

export type NetworkInterfaceIdDataT = { readonly "description": { readonly "en": "Slug of a network_interfaces instance on this node the service binds to (the runtime resolves it against the node's own interfaces). A slug value but a distinct key — it names a relationship, not this object's own id." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "Network interface" } };

const _data: NetworkInterfaceIdDataT = {
	"description": {
		"en": "Slug of a network_interfaces instance on this node the service binds to (the runtime resolves it against the node's own interfaces). A slug value but a distinct key — it names a relationship, not this object's own id."
	},
	"schema": {
		"minLength": 1,
		"type": "string"
	},
	"title": {
		"en": "Network interface"
	}
};
export default _data;
