// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const SlugQualifiedSchema: TSchema = Type.String({"pattern":"^[a-z][a-z0-9_]*$"});

export type SlugQualified = string;

export type SlugQualifiedDataT = { readonly "description": { readonly "en": "Machine identifier unique across an entire instance/site/install — the wire form of `slug` (same snake_case shape) but qualified so no two things on one deployment share it. The addressable name on the bus. Scoped `slug` names a thing within its archetype; this names it install-wide." }; readonly "title": { readonly "en": "Qualified slug" } };

const _data: SlugQualifiedDataT = {
	"description": {
		"en": "Machine identifier unique across an entire instance/site/install — the wire form of `slug` (same snake_case shape) but qualified so no two things on one deployment share it. The addressable name on the bus. Scoped `slug` names a thing within its archetype; this names it install-wide."
	},
	"title": {
		"en": "Qualified slug"
	}
};
export default _data;
