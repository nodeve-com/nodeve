// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const FormatSchema: TSchema = Type.Union([Type.Literal("grouped"), Type.Literal("per_value")], {"type":"string"});

export type Format = "grouped" | "per_value";

export type FormatDataT = { readonly "description": { readonly "en": "Wire layout for decoded points. grouped (default when absent): one retained JSON object per group at <topic_prefix>/<group> = {ts, mono, seq, ...values}. per_value: one retained bare-value topic per point at <topic_prefix>/<group>/<key> (no envelope) — the flat layout legacy per-value subscribers expect. Medium-neutral so any decoder-to-MQTT medium can offer either contract." }; readonly "schema": { readonly "enum": readonly ["grouped", "per_value"]; readonly "type": "string" }; readonly "title": { readonly "en": "Wire format" } };

const _data: FormatDataT = {
	"description": {
		"en": "Wire layout for decoded points. grouped (default when absent): one retained JSON object per group at <topic_prefix>/<group> = {ts, mono, seq, ...values}. per_value: one retained bare-value topic per point at <topic_prefix>/<group>/<key> (no envelope) — the flat layout legacy per-value subscribers expect. Medium-neutral so any decoder-to-MQTT medium can offer either contract."
	},
	"schema": {
		"enum": [
			"grouped",
			"per_value"
		],
		"type": "string"
	},
	"title": {
		"en": "Wire format"
	}
};
export default _data;
