// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const RulesSchema: TSchema = Type.Array(Type.Object({}), {"minItems":1});

export type Rules = Array<Record<string, never>>;

export type RulesDataT = { readonly "description": { readonly "en": "Evaluated in order: first rule whose (register at address & mask) is non-zero wins; its emit string is published. Each rule = { address, mask, emit }." }; readonly "schema": { readonly "items": { readonly "type": "object" }; readonly "minItems": 1; readonly "type": "array" }; readonly "title": { readonly "en": "Priority rules" } };

const _data: RulesDataT = {
	"description": {
		"en": "Evaluated in order: first rule whose (register at address & mask) is non-zero wins; its emit string is published. Each rule = { address, mask, emit }."
	},
	"schema": {
		"items": {
			"type": "object"
		},
		"minItems": 1,
		"type": "array"
	},
	"title": {
		"en": "Priority rules"
	}
};
export default _data;
