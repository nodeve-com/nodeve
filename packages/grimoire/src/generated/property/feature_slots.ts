// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';

export const schema: TSchema = Type.Object({});

export type FeatureSlots = Record<string, never>;

type DataT = { readonly "description": { readonly "en": "The features an archetype assembles: a `<slug>: overlay` map, the key naming the feature bound to that slot (no rename — the key IS the slug). Archetype-level only; a feature pulls a sibling's shape in via `compose`, never by nesting."; readonly "pt": "Os elementos que um arquétipo monta: um mapa `<slug>: overlay`, sendo a chave o elemento ligado a essa ranhura (sem renomear — a chave É o slug). Apenas ao nível de arquétipo." }; readonly "identity": { readonly "archetypeId": "property"; readonly "slug": "feature_slots" }; readonly "title": { readonly "en": "Feature slots"; readonly "pt": "Ranhuras de elemento" } };

const _data: DataT = {
	"description": {
		"en": "The features an archetype assembles: a `<slug>: overlay` map, the key naming the feature bound to that slot (no rename — the key IS the slug). Archetype-level only; a feature pulls a sibling's shape in via `compose`, never by nesting.",
		"pt": "Os elementos que um arquétipo monta: um mapa `<slug>: overlay`, sendo a chave o elemento ligado a essa ranhura (sem renomear — a chave É o slug). Apenas ao nível de arquétipo."
	},
	"identity": {
		"archetypeId": "property",
		"slug": "feature_slots"
	},
	"title": {
		"en": "Feature slots",
		"pt": "Ranhuras de elemento"
	}
};
export const { description, identity, title } = _data;
