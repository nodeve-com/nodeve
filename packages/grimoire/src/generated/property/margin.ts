// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const MarginSchema: TSchema = Type.Number({"exclusiveMinimum":0});

export type Margin = number;

export type MarginDataT = { readonly "description": { readonly "en": "Symmetric ABSOLUTE band around nominal, in the quantity's unit (1500 ± 200 => margin: 200, i.e. 1300-1700). The absolute counterpart to tolerance; use when the datasheet states a ± figure in real units. Mutually exclusive with explicit min/max in practice." }; readonly "schema": { readonly "exclusiveMinimum": 0; readonly "type": "number" }; readonly "title": { readonly "en": "Margin" } };

const _data: MarginDataT = {
	"description": {
		"en": "Symmetric ABSOLUTE band around nominal, in the quantity's unit (1500 ± 200 => margin: 200, i.e. 1300-1700). The absolute counterpart to tolerance; use when the datasheet states a ± figure in real units. Mutually exclusive with explicit min/max in practice."
	},
	"schema": {
		"exclusiveMinimum": 0,
		"type": "number"
	},
	"title": {
		"en": "Margin"
	}
};
export default _data;
