// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const MpptWindowSchema: TSchema = Type.Object({});

export type MpptWindow = Record<string, never>;

export type MpptWindowDataT = { readonly "description": { readonly "en": "The MPPT tracking window, in volts (min = the floor where the converter parks a string once engaged but light is too low to track a real MPP; max = top of the hunt range). A property of the TRACKER, not of any single string — may be narrower than a string's rated voltage envelope. A bare range, not a voltage nominal-range: an MPP window has no rated point to nominate." }; readonly "title": { readonly "en": "MPPT window" } };

const _data: MpptWindowDataT = {
	"description": {
		"en": "The MPPT tracking window, in volts (min = the floor where the converter parks a string once engaged but light is too low to track a real MPP; max = top of the hunt range). A property of the TRACKER, not of any single string — may be narrower than a string's rated voltage envelope. A bare range, not a voltage nominal-range: an MPP window has no rated point to nominate."
	},
	"title": {
		"en": "MPPT window"
	}
};
export default _data;
