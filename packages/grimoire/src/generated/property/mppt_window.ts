// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';

export const schema: TSchema = Type.Object({});

export type MpptWindow = Record<string, never>;

type DataT = { readonly "description": { readonly "en": "The MPPT tracking window, in volts (min = the floor where the converter parks a string once engaged but light is too low to track a real MPP; max = top of the hunt range). A property of the TRACKER, not of any single string — may be narrower than a string's rated voltage envelope. A bare range, not a valued range (valued_range): an MPP window has no rated point to nominate." }; readonly "identity": { readonly "archetypeId": "property"; readonly "slug": "mppt_window" }; readonly "title": { readonly "en": "MPPT window" } };

const _data: DataT = {
	"description": {
		"en": "The MPPT tracking window, in volts (min = the floor where the converter parks a string once engaged but light is too low to track a real MPP; max = top of the hunt range). A property of the TRACKER, not of any single string — may be narrower than a string's rated voltage envelope. A bare range, not a valued range (valued_range): an MPP window has no rated point to nominate."
	},
	"identity": {
		"archetypeId": "property",
		"slug": "mppt_window"
	},
	"title": {
		"en": "MPPT window"
	}
};
export const { description, identity, title } = _data;
