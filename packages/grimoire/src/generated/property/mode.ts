// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const ModeSchema: TSchema = Type.String({"minLength":1});

export type Mode = string;

export type ModeDataT = { readonly "description": { readonly "en": "The intrinsic-region axis: a region of THIS quantity's own axis named by the archetype's operating-state property — an MPPT window (mppt, mppt_fullpower), a circuit operating point (mpp, open_circuit, short_circuit). Open, domain-specific slug. NOT for external drivers that merely gate validity (a run mode) — those are categorical conditions." }; readonly "title": { readonly "en": "Mode" } };

const _data: ModeDataT = {
	"description": {
		"en": "The intrinsic-region axis: a region of THIS quantity's own axis named by the archetype's operating-state property — an MPPT window (mppt, mppt_fullpower), a circuit operating point (mpp, open_circuit, short_circuit). Open, domain-specific slug. NOT for external drivers that merely gate validity (a run mode) — those are categorical conditions."
	},
	"title": {
		"en": "Mode"
	}
};
export default _data;
