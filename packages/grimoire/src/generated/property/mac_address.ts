// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const MacAddressSchema: TSchema = Type.String({"pattern":"^([0-9A-Fa-f]{2}:){5}[0-9A-Fa-f]{2}$"});

export type MacAddress = string;

export type MacAddressDataT = { readonly "description": { readonly "en": "The interface's IEEE 802 hardware address (EUI-48), colon-separated hex — the L2 identity burned into (or assigned to) the NIC." }; readonly "schema": { readonly "pattern": "^([0-9A-Fa-f]{2}:){5}[0-9A-Fa-f]{2}$"; readonly "type": "string" }; readonly "title": { readonly "en": "MAC address" } };

const _data: MacAddressDataT = {
	"description": {
		"en": "The interface's IEEE 802 hardware address (EUI-48), colon-separated hex — the L2 identity burned into (or assigned to) the NIC."
	},
	"schema": {
		"pattern": "^([0-9A-Fa-f]{2}:){5}[0-9A-Fa-f]{2}$",
		"type": "string"
	},
	"title": {
		"en": "MAC address"
	}
};
export default _data;
