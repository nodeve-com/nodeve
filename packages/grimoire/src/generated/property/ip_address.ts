// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const IpAddressSchema: TSchema = Type.String({"minLength":1});

export type IpAddress = string;

export type IpAddressDataT = { readonly "description": { readonly "en": "An L3 address assigned to the interface — an IPv4 dotted-quad (192.0.2.10) or IPv6 (2001:db8::1). The `ip_family` sibling records which. No sensible default — deployment-specific, supplied downstream." }; readonly "title": { readonly "en": "IP address" } };

const _data: IpAddressDataT = {
	"description": {
		"en": "An L3 address assigned to the interface — an IPv4 dotted-quad (192.0.2.10) or IPv6 (2001:db8::1). The `ip_family` sibling records which. No sensible default — deployment-specific, supplied downstream."
	},
	"title": {
		"en": "IP address"
	}
};
export default _data;
