// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const WriteEndpointSchema: TSchema = Type.Integer({"maximum":255,"minimum":0});

export type WriteEndpoint = number;

export type WriteEndpointDataT = { readonly "description": { readonly "en": "OUT endpoint the poll command is written to (8-bit address; high bit encodes direction — 0x01 is OUT 1). Authored as hex." }; readonly "schema": { readonly "maximum": 255; readonly "minimum": 0; readonly "type": "integer" }; readonly "title": { readonly "en": "Write endpoint" } };

const _data: WriteEndpointDataT = {
	"description": {
		"en": "OUT endpoint the poll command is written to (8-bit address; high bit encodes direction — 0x01 is OUT 1). Authored as hex."
	},
	"schema": {
		"maximum": 255,
		"minimum": 0,
		"type": "integer"
	},
	"title": {
		"en": "Write endpoint"
	}
};
export default _data;
