// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const AccessSchema: TSchema = Type.Union([Type.Literal("read"), Type.Literal("write"), Type.Literal("read_write")], {"type":"string"});

export type Access = "read" | "write" | "read_write";

export type AccessDataT = { readonly "description": { readonly "en": "Read, write-only (an action like factory RESET), or both. Most settings are read_write." }; readonly "schema": { readonly "enum": readonly ["read", "write", "read_write"]; readonly "type": "string" }; readonly "title": { readonly "en": "Access" } };

const _data: AccessDataT = {
	"description": {
		"en": "Read, write-only (an action like factory RESET), or both. Most settings are read_write."
	},
	"schema": {
		"enum": [
			"read",
			"write",
			"read_write"
		],
		"type": "string"
	},
	"title": {
		"en": "Access"
	}
};
export default _data;
