// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';

export const schema: TSchema = Type.Object({});

export type Prop = Record<string, never>;

type DataT = { readonly "description": { readonly "en": "The fields a feature groups: a `<name>: overlay` map, the key a bare property slug (`{}` includes it unchanged, an overlay refines it). Feature-level only — a class assembles features, never a bare field."; readonly "pt": "Os campos que um elemento agrupa: um mapa `<name>: overlay`, sendo a chave um slug de propriedade (`{}` inclui-a inalterada, um overlay refina-a). Apenas ao nível de elemento." }; readonly "identity": { readonly "archetypeId": "property"; readonly "slug": "prop" }; readonly "title": { readonly "en": "Prop"; readonly "pt": "Prop" } };

const _data: DataT = {
	"description": {
		"en": "The fields a feature groups: a `<name>: overlay` map, the key a bare property slug (`{}` includes it unchanged, an overlay refines it). Feature-level only — a class assembles features, never a bare field.",
		"pt": "Os campos que um elemento agrupa: um mapa `<name>: overlay`, sendo a chave um slug de propriedade (`{}` inclui-a inalterada, um overlay refina-a). Apenas ao nível de elemento."
	},
	"identity": {
		"archetypeId": "property",
		"slug": "prop"
	},
	"title": {
		"en": "Prop",
		"pt": "Prop"
	}
};
export const { description, identity, title } = _data;
