// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const HysteresisSchema: TSchema = Type.Number({"exclusiveMinimum":0});

export type Hysteresis = number;

export type HysteresisDataT = { readonly "description": { readonly "en": "Optional deadband around `at` to suppress chatter — once tripped, the reading must travel this far back past `at` to clear. Same unit as `at`; never fabricate." }; readonly "schema": { readonly "exclusiveMinimum": 0; readonly "type": "number" }; readonly "title": { readonly "en": "Hysteresis"; readonly "pt": "Histerese" } };

const _data: HysteresisDataT = {
	"description": {
		"en": "Optional deadband around `at` to suppress chatter — once tripped, the reading must travel this far back past `at` to clear. Same unit as `at`; never fabricate."
	},
	"schema": {
		"exclusiveMinimum": 0,
		"type": "number"
	},
	"title": {
		"en": "Hysteresis",
		"pt": "Histerese"
	}
};
export default _data;
