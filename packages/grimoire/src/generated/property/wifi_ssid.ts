// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const WifiSsidSchema: TSchema = Type.String({"maxLength":32,"minLength":1});

export type WifiSsid = string;

export type WifiSsidDataT = { readonly "description": { readonly "en": "The service set identifier (802.11) of the wireless network a station joins — data-link (L2) addressing: which named network, not how we reach the station. No sensible default — deployment-specific." }; readonly "title": { readonly "en": "WiFi SSID" } };

const _data: WifiSsidDataT = {
	"description": {
		"en": "The service set identifier (802.11) of the wireless network a station joins — data-link (L2) addressing: which named network, not how we reach the station. No sensible default — deployment-specific."
	},
	"title": {
		"en": "WiFi SSID"
	}
};
export default _data;
