// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';

export const schema: TSchema = Type.Array(Type.Object({ "range": Type.Array(Type.Integer({"maximum":65535,"minimum":0}), {"maxItems":2,"minItems":2}), "note": Type.String({"minLength":1}) }, {"additionalProperties":false}));

export type InvalidRanges = Array<{ "range": Array<number>; "note": string }>;

type DataT = { readonly "description": { readonly "en": "Inclusive [lo, hi] address ranges a gateway must never bulk-read across, each with a human note of what's generally inside." }; readonly "identity": { readonly "archetype": "property"; readonly "slug": "invalid_ranges" }; readonly "title": { readonly "en": "Invalid ranges" } };

const _data: DataT = {
	"description": {
		"en": "Inclusive [lo, hi] address ranges a gateway must never bulk-read across, each with a human note of what's generally inside."
	},
	"identity": {
		"archetype": "property",
		"slug": "invalid_ranges"
	},
	"title": {
		"en": "Invalid ranges"
	}
};
export const { description, identity, title } = _data;
