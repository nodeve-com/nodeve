// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const InvalidRangesSchema: TSchema = Type.Array(Type.Object({ "range": Type.Array(Type.Integer({"maximum":65535,"minimum":0}), {"maxItems":2,"minItems":2}), "note": Type.String({"minLength":1}) }, {"additionalProperties":false}));

export type InvalidRanges = Array<{ "range": Array<number>; "note": string }>;

export type InvalidRangesDataT = { readonly "description": { readonly "en": "Inclusive [lo, hi] address ranges a gateway must never bulk-read across, each with a human note of what's generally inside." }; readonly "title": { readonly "en": "Invalid ranges" } };

const _data: InvalidRangesDataT = {
	"description": {
		"en": "Inclusive [lo, hi] address ranges a gateway must never bulk-read across, each with a human note of what's generally inside."
	},
	"title": {
		"en": "Invalid ranges"
	}
};
export default _data;
