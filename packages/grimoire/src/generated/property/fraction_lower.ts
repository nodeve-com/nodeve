// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const FractionLowerSchema: TSchema = Type.Number({"exclusiveMinimum":0});

export type FractionLower = number;

export type FractionLowerDataT = { readonly "description": { readonly "en": "Asymmetric FRACTIONAL lower bound as a MULTIPLIER of nominal (0.7 => 0.7×Un). Verbatim spec-sheet form (e.g. 0.7Un–1.2Un). Pairs with `upper_fraction`; use for asymmetric fractional bands the symmetric `tolerance` can't hold. Unit-free." }; readonly "schema": { readonly "exclusiveMinimum": 0; readonly "type": "number" }; readonly "title": { readonly "en": "Lower fraction" } };

const _data: FractionLowerDataT = {
	"description": {
		"en": "Asymmetric FRACTIONAL lower bound as a MULTIPLIER of nominal (0.7 => 0.7×Un). Verbatim spec-sheet form (e.g. 0.7Un–1.2Un). Pairs with `upper_fraction`; use for asymmetric fractional bands the symmetric `tolerance` can't hold. Unit-free."
	},
	"schema": {
		"exclusiveMinimum": 0,
		"type": "number"
	},
	"title": {
		"en": "Lower fraction"
	}
};
export default _data;
