// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const ComposeSchema: TSchema = Type.Union([Type.String({"pattern":"^[a-z][a-z0-9_]*$"}), Type.Array(Type.String({"pattern":"^[a-z][a-z0-9_]*$"}), {"minItems":1})]);

export type Compose = string | Array<string>;

export type ComposeDataT = { readonly "description": { readonly "en": "LITERAL overlay: merge the named siblings' whole resolved defs (props + node data) under this def — a single slug or a list; the outer def wins. The old `alias` (a same-shape rename) is just a one-element compose. Each target must be an object shape; a def groups props, never IS a scalar." }; readonly "title": { readonly "en": "Compose"; readonly "pt": "Compor" } };

const _data: ComposeDataT = {
	"description": {
		"en": "LITERAL overlay: merge the named siblings' whole resolved defs (props + node data) under this def — a single slug or a list; the outer def wins. The old `alias` (a same-shape rename) is just a one-element compose. Each target must be an object shape; a def groups props, never IS a scalar."
	},
	"title": {
		"en": "Compose",
		"pt": "Compor"
	}
};
export default _data;
