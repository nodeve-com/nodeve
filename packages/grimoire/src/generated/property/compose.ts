// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const ComposeSchema: TSchema = Type.Union([Type.String({"pattern":"^[a-z][a-z0-9_]*$"}), Type.Array(Type.String({"pattern":"^[a-z][a-z0-9_]*$"}), {"minItems":1})]);

export type Compose = string | Array<string>;

export type ComposeDataT = { readonly "description": { readonly "en": "FLATTEN the named shapes' props into this def — a single slug or a list. The old `alias` (a same-shape rename) is just a one-element compose. Each target must be an object shape; a def groups props, never IS a scalar." }; readonly "schema": { readonly "anyOf": readonly [{ readonly "pattern": "^[a-z][a-z0-9_]*$"; readonly "type": "string" }, { readonly "items": { readonly "pattern": "^[a-z][a-z0-9_]*$"; readonly "type": "string" }; readonly "minItems": 1; readonly "type": "array" }] }; readonly "title": { readonly "en": "Compose"; readonly "pt": "Compor" } };

const _data: ComposeDataT = {
	"description": {
		"en": "FLATTEN the named shapes' props into this def — a single slug or a list. The old `alias` (a same-shape rename) is just a one-element compose. Each target must be an object shape; a def groups props, never IS a scalar."
	},
	"schema": {
		"anyOf": [
			{
				"pattern": "^[a-z][a-z0-9_]*$",
				"type": "string"
			},
			{
				"items": {
					"pattern": "^[a-z][a-z0-9_]*$",
					"type": "string"
				},
				"minItems": 1,
				"type": "array"
			}
		]
	},
	"title": {
		"en": "Compose",
		"pt": "Compor"
	}
};
export default _data;
