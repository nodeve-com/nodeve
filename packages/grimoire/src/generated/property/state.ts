// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const StateSchema: TSchema = Type.String({"pattern":"^[a-z][a-z0-9_]*$"});

export type State = string;

export type StateDataT = { readonly "description": { readonly "en": "A categorical-observable state group slug (run_state, operating_mode, fault_code), as opposed to a numeric quantity_kind. Declared by a categorical decode entry, referenced by a categorical condition gate — two sides of one channel." }; readonly "title": { readonly "en": "State"; readonly "pt": "Estado" } };

const _data: StateDataT = {
	"description": {
		"en": "A categorical-observable state group slug (run_state, operating_mode, fault_code), as opposed to a numeric quantity_kind. Declared by a categorical decode entry, referenced by a categorical condition gate — two sides of one channel."
	},
	"title": {
		"en": "State",
		"pt": "Estado"
	}
};
export default _data;
