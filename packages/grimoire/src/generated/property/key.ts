// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const KeySchema: TSchema = Type.String({"pattern":"^[A-Za-z0-9_#]+$"});

export type Key = string;

export type KeyDataT = { readonly "description": { readonly "en": "The VE.Direct wire LABEL this field reads (V, VPV, SER#, H19, AC_OUT_V) — the protocol's case-sensitive key, carried as a preserved string value (not the map key, which codegen would camelCase)." }; readonly "schema": { readonly "pattern": "^[A-Za-z0-9_#]+$"; readonly "type": "string" }; readonly "title": { readonly "en": "Wire key"; readonly "pt": "Chave no fio" } };

const _data: KeyDataT = {
	"description": {
		"en": "The VE.Direct wire LABEL this field reads (V, VPV, SER#, H19, AC_OUT_V) — the protocol's case-sensitive key, carried as a preserved string value (not the map key, which codegen would camelCase)."
	},
	"schema": {
		"pattern": "^[A-Za-z0-9_#]+$",
		"type": "string"
	},
	"title": {
		"en": "Wire key",
		"pt": "Chave no fio"
	}
};
export default _data;
