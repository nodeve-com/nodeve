// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';

export const schema: TSchema = Type.Union([Type.Literal("above"), Type.Literal("below")], {"type":"string"});

export type TriggerOn = "above" | "below";

type DataT = { readonly "description": { readonly "en": "Which side turns an `interval_kind: threshold` ON — the one irreducible bit a hysteretic trigger needs (min/max alone give the two edges, not which region is ON). `below` ⇒ ON when the reading is below `min`, OFF once above `max` (a heat demand: on cold, off warm). `above` ⇒ ON above `max`, OFF below `min` (a cool demand / over-limit trip). Between the edges the boolean HOLDS its prior state — the deadband `[min,max]` is what makes it stateful. Its presence is what derives `interval_kind: threshold`. Borrowed from HA numeric_state (above/below)." }; readonly "identity": { readonly "archetypeId": "property"; readonly "slug": "trigger_on" }; readonly "title": { readonly "en": "Trigger on"; readonly "pt": "Aciona em" } };

const _data: DataT = {
	"description": {
		"en": "Which side turns an `interval_kind: threshold` ON — the one irreducible bit a hysteretic trigger needs (min/max alone give the two edges, not which region is ON). `below` ⇒ ON when the reading is below `min`, OFF once above `max` (a heat demand: on cold, off warm). `above` ⇒ ON above `max`, OFF below `min` (a cool demand / over-limit trip). Between the edges the boolean HOLDS its prior state — the deadband `[min,max]` is what makes it stateful. Its presence is what derives `interval_kind: threshold`. Borrowed from HA numeric_state (above/below)."
	},
	"identity": {
		"archetypeId": "property",
		"slug": "trigger_on"
	},
	"title": {
		"en": "Trigger on",
		"pt": "Aciona em"
	}
};
export const { description, identity, title } = _data;
