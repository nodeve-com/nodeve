// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const BroaderSchema: TSchema = Type.String({"pattern":"^[a-z][a-z0-9_]*$"});

export type Broader = string;

export type BroaderDataT = { readonly "description": { readonly "en": "Intra-vocabulary generalization (skos:broader) — the code of a more general concept in the same vocabulary this one specializes (active_power → power). Distinct from refs (cross-vocabulary crosswalks)." }; readonly "refs": readonly [{ readonly "match": "exact"; readonly "registryId": "skos"; readonly "term": "broader" }]; readonly "title": { readonly "en": "Broader"; readonly "pt": "Mais amplo" } };

const _data: BroaderDataT = {
	"description": {
		"en": "Intra-vocabulary generalization (skos:broader) — the code of a more general concept in the same vocabulary this one specializes (active_power → power). Distinct from refs (cross-vocabulary crosswalks)."
	},
	"refs": [
		{
			"match": "exact",
			"registryId": "skos",
			"term": "broader"
		}
	],
	"title": {
		"en": "Broader",
		"pt": "Mais amplo"
	}
};
export default _data;
