// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const ValueOffsetSchema: TSchema = Type.Integer({"minimum":0});

export type ValueOffset = number;

export type ValueOffsetDataT = { readonly "description": { readonly "en": "Where the VALUE bytes sit — in the GET response (decode from here) and the SET request (encode to here), big-endian per the parameter's byte_order (M4-ATX: 4)." }; readonly "schema": { readonly "minimum": 0; readonly "type": "integer" }; readonly "title": { readonly "en": "Value offset" } };

const _data: ValueOffsetDataT = {
	"description": {
		"en": "Where the VALUE bytes sit — in the GET response (decode from here) and the SET request (encode to here), big-endian per the parameter's byte_order (M4-ATX: 4)."
	},
	"schema": {
		"minimum": 0,
		"type": "integer"
	},
	"title": {
		"en": "Value offset"
	}
};
export default _data;
