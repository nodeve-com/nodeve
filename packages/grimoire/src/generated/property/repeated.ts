// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const RepeatedSchema: TSchema = Type.Boolean();

export type Repeated = boolean;

export type RepeatedDataT = { readonly "description": { readonly "en": "The feature's intrinsic cardinality: true = countable instances of this feature's shape, wrapped in an envelope `{count, combined?, default?, instances?}` — NOT a bare list (that is `array`). The envelope carries a `count`, an optional `combined` aggregate over all instances, a `default` applied to each, and optional per-instance `instances` joined by `ordinal`; a metered collection may state count/combined with no `instances` enumerated at all. Exclusive with `part` (a fixed named-parts map) and with `array` (a bare list)." }; readonly "title": { readonly "en": "Repeated"; readonly "pt": "Repetido" } };

const _data: RepeatedDataT = {
	"description": {
		"en": "The feature's intrinsic cardinality: true = countable instances of this feature's shape, wrapped in an envelope `{count, combined?, default?, instances?}` — NOT a bare list (that is `array`). The envelope carries a `count`, an optional `combined` aggregate over all instances, a `default` applied to each, and optional per-instance `instances` joined by `ordinal`; a metered collection may state count/combined with no `instances` enumerated at all. Exclusive with `part` (a fixed named-parts map) and with `array` (a bare list)."
	},
	"title": {
		"en": "Repeated",
		"pt": "Repetido"
	}
};
export default _data;
