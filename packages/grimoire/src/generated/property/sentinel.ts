// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';

export const schema: TSchema = Type.Object({ "raw": Type.Optional(Type.Integer({"minimum":0})), "meaning": Type.Optional(Type.String({"pattern":"^[a-z][a-z0-9_]*$"})) }, {"additionalProperties":false});

export type Sentinel = { "raw"?: number; "meaning"?: string };

type DataT = { readonly "description": { readonly "en": "A reserved raw value with a categorical meaning instead of a number (M4-ATX timers use 0xffff for never). raw == reading ⇒ emit the meaning slug rather than scaling; a writer maps back. The reserved reading is named raw, not value." }; readonly "identity": { readonly "archetype": "property"; readonly "slug": "sentinel" }; readonly "title": { readonly "en": "Sentinel" } };

const _data: DataT = {
	"description": {
		"en": "A reserved raw value with a categorical meaning instead of a number (M4-ATX timers use 0xffff for never). raw == reading ⇒ emit the meaning slug rather than scaling; a writer maps back. The reserved reading is named raw, not value."
	},
	"identity": {
		"archetype": "property",
		"slug": "sentinel"
	},
	"title": {
		"en": "Sentinel"
	}
};
export const { description, identity, title } = _data;
