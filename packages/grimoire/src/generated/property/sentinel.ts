// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const SentinelSchema: TSchema = Type.Object({ "raw": Type.Optional(Type.Integer({"minimum":0})), "meaning": Type.Optional(Type.String({"pattern":"^[a-z][a-z0-9_]*$"})) }, {"additionalProperties":false});

export type Sentinel = { "raw"?: number; "meaning"?: string };

export type SentinelDataT = { readonly "description": { readonly "en": "A reserved raw value with a categorical meaning instead of a number (M4-ATX timers use 0xffff for never). raw == reading ⇒ emit the meaning slug rather than scaling; a writer maps back. The reserved reading is named raw, not value." }; readonly "title": { readonly "en": "Sentinel" } };

const _data: SentinelDataT = {
	"description": {
		"en": "A reserved raw value with a categorical meaning instead of a number (M4-ATX timers use 0xffff for never). raw == reading ⇒ emit the meaning slug rather than scaling; a writer maps back. The reserved reading is named raw, not value."
	},
	"title": {
		"en": "Sentinel"
	}
};
export default _data;
