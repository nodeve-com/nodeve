// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';

export const schema: TSchema = Type.Array(Type.Unknown(), {"minItems":1});

export type Enums = Array<unknown>;

type DataT = { readonly "description": { readonly "en": "The enumerations a feature binds — each entry names an `enumeration/<name>` directory, resolving to that dir's member stems as an enum of string literals. Feature-level only; an enum reaches a class inside a feature, never directly."; readonly "pt": "As enumerações que um elemento associa — cada entrada nomeia um diretório `enumeration/<name>`, resolvendo para os stems dos seus membros como um enum de literais de string. Apenas ao nível de elemento." }; readonly "identity": { readonly "archetypeId": "property"; readonly "slug": "enums" }; readonly "title": { readonly "en": "Enums"; readonly "pt": "Enums" } };

const _data: DataT = {
	"description": {
		"en": "The enumerations a feature binds — each entry names an `enumeration/<name>` directory, resolving to that dir's member stems as an enum of string literals. Feature-level only; an enum reaches a class inside a feature, never directly.",
		"pt": "As enumerações que um elemento associa — cada entrada nomeia um diretório `enumeration/<name>`, resolvendo para os stems dos seus membros como um enum de literais de string. Apenas ao nível de elemento."
	},
	"identity": {
		"archetypeId": "property",
		"slug": "enums"
	},
	"title": {
		"en": "Enums",
		"pt": "Enums"
	}
};
export const { description, identity, title } = _data;
