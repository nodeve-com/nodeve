// GENERATED by `pnpm generate` from concepts/property/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';

export const LabelsSchema: TSchema = Type.Array(Type.Array(Type.Unknown(), {"maxItems":16}), {"minItems":1});

export type Labels = Array<Array<unknown>>;

export type LabelsDataT = { readonly "description": { readonly "en": "One label per bit, index = bit position (matched as value & (1 << index)); null marks a reserved/unused bit. 0-16 entries per register word; may be shorter than 16 (trailing unmapped bits omitted) or empty. Outer array parallel to addresses." }; readonly "schema": { readonly "items": { readonly "items": { readonly "type": readonly ["string", "null"] }; readonly "maxItems": 16; readonly "type": "array" }; readonly "minItems": 1; readonly "type": "array" }; readonly "title": { readonly "en": "Bit labels" } };

const _data: LabelsDataT = {
	"description": {
		"en": "One label per bit, index = bit position (matched as value & (1 << index)); null marks a reserved/unused bit. 0-16 entries per register word; may be shorter than 16 (trailing unmapped bits omitted) or empty. Outer array parallel to addresses."
	},
	"schema": {
		"items": {
			"items": {
				"type": [
					"string",
					"null"
				]
			},
			"maxItems": 16,
			"type": "array"
		},
		"minItems": 1,
		"type": "array"
	},
	"title": {
		"en": "Bit labels"
	}
};
export default _data;
