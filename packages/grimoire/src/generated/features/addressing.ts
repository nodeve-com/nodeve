// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import PasswordData, { PasswordSchema, type Password, type PasswordDataT } from '../property/password.ts';
import WifiSsidData, { WifiSsidSchema, type WifiSsid, type WifiSsidDataT } from '../property/wifi_ssid.ts';

export const AddressingSchema: TSchema = Type.Object({ "wifi_ssid": Type.Optional(WifiSsidSchema), "password": Type.Optional(PasswordSchema) }, {"additionalProperties":false});

export type Addressing = { "wifiSsid"?: WifiSsid; "password"?: Password };

export type AddressingDataT = { readonly "prop": { readonly "password": Omit<PasswordDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "The pre-shared key (WPA PSK) the station presents to join the WiFi network named by `wifi_ssid`. A deployment secret — supplied downstream, never a catalog default." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "WiFi password" } }; readonly "wifi_ssid": Omit<WifiSsidDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "The service set identifier (802.11) of the wireless network a station joins — data-link (L2) addressing: which named network, not how we reach the station. No sensible default — deployment-specific." }; readonly "schema": { readonly "maxLength": 32; readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "WiFi SSID" } } }; readonly "title": { readonly "en": "Addressing" } };

const _data: AddressingDataT = {
	"prop": {
		"password": {
			...PasswordData,
			"description": {
				"en": "The pre-shared key (WPA PSK) the station presents to join the WiFi network named by `wifi_ssid`. A deployment secret — supplied downstream, never a catalog default."
			},
			"schema": {
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "WiFi password"
			}
		},
		"wifi_ssid": {
			...WifiSsidData,
			"description": {
				"en": "The service set identifier (802.11) of the wireless network a station joins — data-link (L2) addressing: which named network, not how we reach the station. No sensible default — deployment-specific."
			},
			"schema": {
				"maxLength": 32,
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "WiFi SSID"
			}
		}
	},
	"title": {
		"en": "Addressing"
	}
};
export default _data;
