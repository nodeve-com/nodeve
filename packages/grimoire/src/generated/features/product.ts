// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';
import * as manufacturerId_ from '../property/manufacturer_id.ts';

export const schema: TSchema = Type.Object({ "manufacturerId": manufacturerId_.schema, "model": Type.String({"minLength":1}) }, {"additionalProperties":false,"x-key-map":{"manufacturer_id":"manufacturerId"}});

export type Product = { "manufacturerId": manufacturerId_.ManufacturerId; "model": string };

type DataT = { readonly "description": { readonly "en": "Product identity — who made the thing and what it's called; lives under its own `product` slot, not a feature spec."; readonly "pt": "Identidade do produto — quem o fez e como se chama; vive sob o seu próprio slot `product`, não é uma especificação de recurso." }; readonly "identity": { readonly "archetype": "feature"; readonly "slug": "product" }; readonly "prop": { readonly "manufacturerId": typeof manufacturerId_; readonly "model": { readonly "description": { readonly "en": "What the thing is called — the model designation on the nameplate."; readonly "pt": "Como a coisa se chama — a designação de modelo na placa de identificação." }; readonly "title": { readonly "en": "Model"; readonly "pt": "Modelo" } } }; readonly "title": { readonly "en": "Product"; readonly "pt": "Produto" } };

const _data: DataT = {
	"description": {
		"en": "Product identity — who made the thing and what it's called; lives under its own `product` slot, not a feature spec.",
		"pt": "Identidade do produto — quem o fez e como se chama; vive sob o seu próprio slot `product`, não é uma especificação de recurso."
	},
	"identity": {
		"archetype": "feature",
		"slug": "product"
	},
	"prop": {
		"manufacturerId": manufacturerId_,
		"model": {
			"description": {
				"en": "What the thing is called — the model designation on the nameplate.",
				"pt": "Como a coisa se chama — a designação de modelo na placa de identificação."
			},
			"title": {
				"en": "Model",
				"pt": "Modelo"
			}
		}
	},
	"title": {
		"en": "Product",
		"pt": "Produto"
	}
};
export const { description, identity, prop, title } = _data;
