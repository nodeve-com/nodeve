// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import ConnectTimeoutMsData, { ConnectTimeoutMsSchema, type ConnectTimeoutMs, type ConnectTimeoutMsDataT } from '../property/connect_timeout_ms.ts';
import HostData, { HostSchema, type Host, type HostDataT } from '../property/host.ts';
import PathnameData, { PathnameSchema, type Pathname, type PathnameDataT } from '../property/pathname.ts';
import PortData, { PortSchema, type Port, type PortDataT } from '../property/port.ts';
import ReconnectPeriodMsData, { ReconnectPeriodMsSchema, type ReconnectPeriodMs, type ReconnectPeriodMsDataT } from '../property/reconnect_period_ms.ts';
import VersionData, { VersionSchema, type Version, type VersionDataT } from '../property/version.ts';

export const EndpointSchema: TSchema = Type.Object({ "transport_protocol": Type.Optional(Type.Union([Type.Literal("tcp"), Type.Literal("udp")], {"type":"string"})), "host": Type.Optional(HostSchema), "port": Type.Optional(PortSchema), "connect_timeout_ms": Type.Optional(ConnectTimeoutMsSchema), "reconnect_period_ms": Type.Optional(ReconnectPeriodMsSchema), "service_protocol": Type.Optional(Type.Union([Type.Literal("mqtt"), Type.Literal("mqtts"), Type.Literal("http"), Type.Literal("https"), Type.Literal("ws"), Type.Literal("wss")], {"type":"string"})), "pathname": Type.Optional(PathnameSchema), "version": Type.Optional(VersionSchema) }, {"additionalProperties":false,"allOf":[{"if":{"required":["service_protocol"]},"then":{"properties":{"transport_protocol":{"const":"tcp"}}}},{"if":{"required":["transport_protocol"],"properties":{"transport_protocol":{"const":"tcp"}}},"then":{"required":["connect_timeout_ms","reconnect_period_ms"]}},{"if":{"required":["transport_protocol"],"properties":{"transport_protocol":{"const":"udp"}}},"then":{"properties":{"connect_timeout_ms":false,"reconnect_period_ms":false}}}]});

export type Endpoint = { "transportProtocol"?: "tcp" | "udp"; "host"?: Host; "port"?: Port; "connectTimeoutMs"?: ConnectTimeoutMs; "reconnectPeriodMs"?: ReconnectPeriodMs; "serviceProtocol"?: "mqtt" | "mqtts" | "http" | "https" | "ws" | "wss"; "pathname"?: Pathname; "version"?: Version };

export type EndpointDataT = { readonly "description": { readonly "en": "One way to reach a node — a URL when a service_protocol scheme is set (rendered ${scheme}://${host}:${port}${pathname}), or a raw tcp/udp socket when it isn't (bare ${host}:${port}). Reachability comes from socket_address; dial policy from reconnect. Every field optional, falling back to shared/well-known defaults." }; readonly "prop": { readonly "connect_timeout_ms": Omit<ConnectTimeoutMsDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "How long a connection attempt waits before giving up, integer milliseconds. Distinct from the per-transfer timeout_ms." }; readonly "schema": { readonly "exclusiveMinimum": 0; readonly "type": "integer" }; readonly "title": { readonly "en": "Connect timeout (ms)"; readonly "pt": "Timeout de conexão (ms)" } }; readonly "host": Omit<HostDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Hostname or IP an endpoint is reached at (the remote address a dialer connects out to). No sensible default — deployment-specific, supplied downstream." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "Host" } }; readonly "pathname": Omit<PathnameDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "A slash-delimited path a resource is reached or published under (like URL.pathname): the URL path an endpoint serves on (WebSocket schemes use it, TCP omit it — bare / when absent), or the shared root a set of publishers nest their paths beneath (<pathname>/<slug>)." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "Pathname" } }; readonly "port": Omit<PortDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "TCP/UDP port number. Omit to fall back to the scheme's well-known default." }; readonly "schema": { readonly "maximum": 65535; readonly "minimum": 1; readonly "type": "integer" }; readonly "title": { readonly "en": "Port" } }; readonly "reconnect_period_ms": Omit<ReconnectPeriodMsDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Delay before a dropped connection is retried, integer milliseconds." }; readonly "schema": { readonly "exclusiveMinimum": 0; readonly "type": "integer" }; readonly "title": { readonly "en": "Reconnect period (ms)"; readonly "pt": "Período de reconexão (ms)" } }; readonly "service_protocol": { readonly "schema": { readonly "enum": readonly ["mqtt", "mqtts", "http", "https", "ws", "wss"]; readonly "type": "string" } }; readonly "transport_protocol": { readonly "schema": { readonly "enum": readonly ["tcp", "udp"]; readonly "type": "string" } }; readonly "version": Omit<VersionDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Wire-protocol version an endpoint speaks (e.g. MQTT 5 vs 3.1.1). Omit to fall back to the scheme's default." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "Protocol version" } } }; readonly "schema": { readonly "allOf": readonly [{ readonly "if": { readonly "required": readonly ["service_protocol"] }; readonly "then": { readonly "properties": { readonly "transport_protocol": { readonly "const": "tcp" } } } }, { readonly "if": { readonly "properties": { readonly "transport_protocol": { readonly "const": "tcp" } }; readonly "required": readonly ["transport_protocol"] }; readonly "then": { readonly "required": readonly ["connect_timeout_ms", "reconnect_period_ms"] } }, { readonly "if": { readonly "properties": { readonly "transport_protocol": { readonly "const": "udp" } }; readonly "required": readonly ["transport_protocol"] }; readonly "then": { readonly "properties": { readonly "connect_timeout_ms": false; readonly "reconnect_period_ms": false } } }] }; readonly "title": { readonly "en": "Endpoint" } };

const _data: EndpointDataT = {
	"description": {
		"en": "One way to reach a node — a URL when a service_protocol scheme is set (rendered ${scheme}://${host}:${port}${pathname}), or a raw tcp/udp socket when it isn't (bare ${host}:${port}). Reachability comes from socket_address; dial policy from reconnect. Every field optional, falling back to shared/well-known defaults."
	},
	"prop": {
		"connect_timeout_ms": {
			...ConnectTimeoutMsData,
			"description": {
				"en": "How long a connection attempt waits before giving up, integer milliseconds. Distinct from the per-transfer timeout_ms."
			},
			"schema": {
				"exclusiveMinimum": 0,
				"type": "integer"
			},
			"title": {
				"en": "Connect timeout (ms)",
				"pt": "Timeout de conexão (ms)"
			}
		},
		"host": {
			...HostData,
			"description": {
				"en": "Hostname or IP an endpoint is reached at (the remote address a dialer connects out to). No sensible default — deployment-specific, supplied downstream."
			},
			"schema": {
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "Host"
			}
		},
		"pathname": {
			...PathnameData,
			"description": {
				"en": "A slash-delimited path a resource is reached or published under (like URL.pathname): the URL path an endpoint serves on (WebSocket schemes use it, TCP omit it — bare / when absent), or the shared root a set of publishers nest their paths beneath (<pathname>/<slug>)."
			},
			"schema": {
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "Pathname"
			}
		},
		"port": {
			...PortData,
			"description": {
				"en": "TCP/UDP port number. Omit to fall back to the scheme's well-known default."
			},
			"schema": {
				"maximum": 65535,
				"minimum": 1,
				"type": "integer"
			},
			"title": {
				"en": "Port"
			}
		},
		"reconnect_period_ms": {
			...ReconnectPeriodMsData,
			"description": {
				"en": "Delay before a dropped connection is retried, integer milliseconds."
			},
			"schema": {
				"exclusiveMinimum": 0,
				"type": "integer"
			},
			"title": {
				"en": "Reconnect period (ms)",
				"pt": "Período de reconexão (ms)"
			}
		},
		"service_protocol": {
			"schema": {
				"enum": [
					"mqtt",
					"mqtts",
					"http",
					"https",
					"ws",
					"wss"
				],
				"type": "string"
			}
		},
		"transport_protocol": {
			"schema": {
				"enum": [
					"tcp",
					"udp"
				],
				"type": "string"
			}
		},
		"version": {
			...VersionData,
			"description": {
				"en": "Wire-protocol version an endpoint speaks (e.g. MQTT 5 vs 3.1.1). Omit to fall back to the scheme's default."
			},
			"schema": {
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "Protocol version"
			}
		}
	},
	"schema": {
		"allOf": [
			{
				"if": {
					"required": [
						"service_protocol"
					]
				},
				"then": {
					"properties": {
						"transport_protocol": {
							"const": "tcp"
						}
					}
				}
			},
			{
				"if": {
					"properties": {
						"transport_protocol": {
							"const": "tcp"
						}
					},
					"required": [
						"transport_protocol"
					]
				},
				"then": {
					"required": [
						"connect_timeout_ms",
						"reconnect_period_ms"
					]
				}
			},
			{
				"if": {
					"properties": {
						"transport_protocol": {
							"const": "udp"
						}
					},
					"required": [
						"transport_protocol"
					]
				},
				"then": {
					"properties": {
						"connect_timeout_ms": false,
						"reconnect_period_ms": false
					}
				}
			}
		]
	},
	"title": {
		"en": "Endpoint"
	}
};
export default _data;
