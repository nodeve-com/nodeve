// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';
import * as references_ from '../property/references.ts';

export const schema: TSchema = Type.Object({ "references": Type.Optional(references_.schema) }, {"additionalProperties":false});

export type Column = { "references"?: references_.References };

type DataT = { readonly "description": { readonly "en": "Relational metadata a property projects to as a DB column (foreign key, …)."; readonly "pt": "Metadados relacionais que uma propriedade projeta como coluna de BD (chave estrangeira, …)." }; readonly "identity": { readonly "archetype": "feature"; readonly "slug": "column" }; readonly "prop": { readonly "references": typeof references_ }; readonly "title": { readonly "en": "Column"; readonly "pt": "Coluna" } };

const _data: DataT = {
	"description": {
		"en": "Relational metadata a property projects to as a DB column (foreign key, …).",
		"pt": "Metadados relacionais que uma propriedade projeta como coluna de BD (chave estrangeira, …)."
	},
	"identity": {
		"archetype": "feature",
		"slug": "column"
	},
	"prop": {
		"references": references_
	},
	"title": {
		"en": "Column",
		"pt": "Coluna"
	}
};
export const { description, identity, prop, title } = _data;
