// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import ReferencesData, { ReferencesSchema, type References, type ReferencesDataT } from '../property/references.ts';

export const ColumnSchema: TSchema = Type.Object({ "references": Type.Optional(ReferencesSchema) }, {"additionalProperties":false});

export type Column = { "references"?: References };

export type ColumnDataT = { readonly "description": { readonly "en": "Relational metadata a property projects to as a DB column (foreign key, …)."; readonly "pt": "Metadados relacionais que uma propriedade projeta como coluna de BD (chave estrangeira, …)." }; readonly "prop": { readonly "references": Omit<ReferencesDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Archetype whose identity.slug this field's value must be — the field's foreign-key target."; readonly "pt": "Arquétipo cujo identity.slug o valor deste campo deve ser — o alvo de chave estrangeira do campo." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "References"; readonly "pt": "Referencia" } } }; readonly "title": { readonly "en": "Column"; readonly "pt": "Coluna" } };

const _data: ColumnDataT = {
	"description": {
		"en": "Relational metadata a property projects to as a DB column (foreign key, …).",
		"pt": "Metadados relacionais que uma propriedade projeta como coluna de BD (chave estrangeira, …)."
	},
	"prop": {
		"references": {
			...ReferencesData,
			"description": {
				"en": "Archetype whose identity.slug this field's value must be — the field's foreign-key target.",
				"pt": "Arquétipo cujo identity.slug o valor deste campo deve ser — o alvo de chave estrangeira do campo."
			},
			"schema": {
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "References",
				"pt": "Referencia"
			}
		}
	},
	"title": {
		"en": "Column",
		"pt": "Coluna"
	}
};
export default _data;
