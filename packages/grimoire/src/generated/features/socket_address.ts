// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import HostData, { HostSchema, type Host, type HostDataT } from '../property/host.ts';
import PortData, { PortSchema, type Port, type PortDataT } from '../property/port.ts';

export const SocketAddressSchema: TSchema = Type.Object({ "transport_protocol": Type.Optional(Type.Union([Type.Literal("tcp"), Type.Literal("udp")], {"type":"string"})), "host": Type.Optional(HostSchema), "port": Type.Optional(PortSchema) }, {"additionalProperties":false});

export type SocketAddress = { "transportProtocol"?: "tcp" | "udp"; "host"?: Host; "port"?: Port };

export type SocketAddressDataT = { readonly "prop": { readonly "host": Omit<HostDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Hostname or IP an endpoint is reached at (the remote address a dialer connects out to). No sensible default — deployment-specific, supplied downstream." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "Host" } }; readonly "port": Omit<PortDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "TCP/UDP port number. Omit to fall back to the scheme's well-known default." }; readonly "schema": { readonly "maximum": 65535; readonly "minimum": 1; readonly "type": "integer" }; readonly "title": { readonly "en": "Port" } }; readonly "transport_protocol": { readonly "schema": { readonly "enum": readonly ["tcp", "udp"]; readonly "type": "string" } } } };

const _data: SocketAddressDataT = {
	"prop": {
		"host": {
			...HostData,
			"description": {
				"en": "Hostname or IP an endpoint is reached at (the remote address a dialer connects out to). No sensible default — deployment-specific, supplied downstream."
			},
			"schema": {
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "Host"
			}
		},
		"port": {
			...PortData,
			"description": {
				"en": "TCP/UDP port number. Omit to fall back to the scheme's well-known default."
			},
			"schema": {
				"maximum": 65535,
				"minimum": 1,
				"type": "integer"
			},
			"title": {
				"en": "Port"
			}
		},
		"transport_protocol": {
			"schema": {
				"enum": [
					"tcp",
					"udp"
				],
				"type": "string"
			}
		}
	}
};
export default _data;
