// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';

export const schema: TSchema = Type.Object({});

export type Schema = Record<string, never>;

type DataT = { readonly "body": { readonly "en": "The PROJECTION PASSTHROUGH: kit/project.ts merges an object node's `schema:` into its emitted object schema, and takes a leaf's verbatim as the field's shape. Two roles, one key — a property's value-shape (`type`/`minimum`/`pattern`, the counterpart to `column:`'s relational facts), and an object node's cross-field invariants (if/then/else, oneOf, dependentRequired — see inet_socket.yaml: transport==tcp ⇒ reconnect fields required). NEVER coin a concept_settings primitive for a rule JSON Schema already expresses.\n\nComposed onto `thing`, so EVERY layer may carry it — property, feature and archetype defs all do. Declared here rather than per-layer meta-def: one definition, not three.\n\nIts contents are draft-07 keywords, an open set the validator hands to Ajv rather than enumerating — hence the open object below. What is constrained is WHERE it may appear, which the composing meta-def decides.\n" }; readonly "description": { readonly "en": "A node's own raw draft-07 block — its value-shape as a property, or its cross-field constraints as a grouping."; readonly "pt": "O bloco draft-07 do próprio nó — a forma do seu valor como propriedade, ou as suas restrições entre campos como agrupamento." }; readonly "identity": { readonly "archetypeId": "feature"; readonly "slug": "schema" }; readonly "title": { readonly "en": "Schema"; readonly "pt": "Esquema" } };

const _data: DataT = {
	"body": {
		"en": "The PROJECTION PASSTHROUGH: kit/project.ts merges an object node's `schema:` into its emitted object schema, and takes a leaf's verbatim as the field's shape. Two roles, one key — a property's value-shape (`type`/`minimum`/`pattern`, the counterpart to `column:`'s relational facts), and an object node's cross-field invariants (if/then/else, oneOf, dependentRequired — see inet_socket.yaml: transport==tcp ⇒ reconnect fields required). NEVER coin a concept_settings primitive for a rule JSON Schema already expresses.\n\nComposed onto `thing`, so EVERY layer may carry it — property, feature and archetype defs all do. Declared here rather than per-layer meta-def: one definition, not three.\n\nIts contents are draft-07 keywords, an open set the validator hands to Ajv rather than enumerating — hence the open object below. What is constrained is WHERE it may appear, which the composing meta-def decides.\n"
	},
	"description": {
		"en": "A node's own raw draft-07 block — its value-shape as a property, or its cross-field constraints as a grouping.",
		"pt": "O bloco draft-07 do próprio nó — a forma do seu valor como propriedade, ou as suas restrições entre campos como agrupamento."
	},
	"identity": {
		"archetypeId": "feature",
		"slug": "schema"
	},
	"title": {
		"en": "Schema",
		"pt": "Esquema"
	}
};
export const { body, description, identity, title } = _data;
