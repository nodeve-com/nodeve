// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import HeightData, { HeightSchema, type Height, type HeightDataT } from '../property/height.ts';
import MassData, { MassSchema, type Mass, type MassDataT } from '../property/mass.ts';
import ThicknessData, { ThicknessSchema, type Thickness, type ThicknessDataT } from '../property/thickness.ts';
import WidthData, { WidthSchema, type Width, type WidthDataT } from '../property/width.ts';

export const PhysicalSchema: TSchema = Type.Object({ "width": Type.Optional(WidthSchema), "height": Type.Optional(HeightSchema), "thickness": Type.Optional(ThicknessSchema), "mass": Type.Optional(MassSchema) }, {"additionalProperties":false});

export type Physical = { "width"?: Width; "height"?: Height; "thickness"?: Thickness; "mass"?: Mass };

export type PhysicalDataT = { readonly "prop": { readonly "height": HeightDataT; readonly "mass": MassDataT; readonly "thickness": ThicknessDataT; readonly "width": WidthDataT } };

const _data: PhysicalDataT = {
	"prop": {
		"height": HeightData,
		"mass": MassData,
		"thickness": ThicknessData,
		"width": WidthData
	}
};
export default _data;
