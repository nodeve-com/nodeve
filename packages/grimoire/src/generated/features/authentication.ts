// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import PasswordData, { PasswordSchema, type Password, type PasswordDataT } from '../property/password.ts';
import UsernameData, { UsernameSchema, type Username, type UsernameDataT } from '../property/username.ts';

export const AuthenticationSchema: TSchema = Type.Object({ "username": Type.Optional(UsernameSchema), "password": Type.Optional(PasswordSchema) }, {"additionalProperties":false});

export type Authentication = { "username"?: Username; "password"?: Password };

export type AuthenticationDataT = { readonly "prop": { readonly "password": Omit<PasswordDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Secret presented alongside username when a connection authenticates. A deployment secret — supplied downstream, never a catalog default." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "Password" } }; readonly "username": Omit<UsernameDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Login name presented when a connection authenticates to a broker/server. Omit for anonymous connections. Paired with password." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "Username" } } } };

const _data: AuthenticationDataT = {
	"prop": {
		"password": {
			...PasswordData,
			"description": {
				"en": "Secret presented alongside username when a connection authenticates. A deployment secret — supplied downstream, never a catalog default."
			},
			"schema": {
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "Password"
			}
		},
		"username": {
			...UsernameData,
			"description": {
				"en": "Login name presented when a connection authenticates to a broker/server. Omit for anonymous connections. Paired with password."
			},
			"schema": {
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "Username"
			}
		}
	}
};
export default _data;
