// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';
import * as siUnit_ from '../property/si_unit.ts';
import * as unit_ from '../property/unit.ts';

export const schema: TSchema = Type.Object({ "accumulation": Type.Optional(Type.Union([Type.Literal("cumulative"), Type.Literal("cumulative_monotonic"), Type.Literal("instantaneous")], {"type":"string"})), "unit": Type.Optional(unit_.schema), "siUnit": Type.Optional(siUnit_.schema) }, {"additionalProperties":false,"x-key-map":{"si_unit":"siUnit"}});

export type Measurand = { "accumulation"?: "cumulative" | "cumulative_monotonic" | "instantaneous"; "unit"?: unit_.Unit; "siUnit"?: siUnit_.SiUnit };

type DataT = { readonly "description": { readonly "en": "The metrology facts of a quantity kind — its unit, SI unit, and default accumulation. The kind's enum member carries the site-agnostic defaults; a `specification` composes measurand so a device/site can OVERRIDE them. The deterministic on-bus `slug` lives on the specification's `identity`, not here." }; readonly "identity": { readonly "archetypeId": "feature"; readonly "slug": "measurand" }; readonly "prop": { readonly "siUnit": typeof siUnit_; readonly "unit": typeof unit_ }; readonly "title": { readonly "en": "Measurand" } };

const _data: DataT = {
	"description": {
		"en": "The metrology facts of a quantity kind — its unit, SI unit, and default accumulation. The kind's enum member carries the site-agnostic defaults; a `specification` composes measurand so a device/site can OVERRIDE them. The deterministic on-bus `slug` lives on the specification's `identity`, not here."
	},
	"identity": {
		"archetypeId": "feature",
		"slug": "measurand"
	},
	"prop": {
		"siUnit": siUnit_,
		"unit": unit_
	},
	"title": {
		"en": "Measurand"
	}
};
export const { description, identity, prop, title } = _data;
