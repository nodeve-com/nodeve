// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import SiUnitData, { SiUnitSchema, type SiUnit, type SiUnitDataT } from '../property/si_unit.ts';
import UnitData, { UnitSchema, type Unit, type UnitDataT } from '../property/unit.ts';

export const MeasurandSchema: TSchema = Type.Object({ "accumulation": Type.Optional(Type.Union([Type.Literal("cumulative"), Type.Literal("cumulative_monotonic"), Type.Literal("instantaneous")], {"type":"string"})), "unit": Type.Optional(UnitSchema), "si_unit": Type.Optional(SiUnitSchema) }, {"additionalProperties":false});

export type Measurand = { "accumulation"?: "cumulative" | "cumulative_monotonic" | "instantaneous"; "unit"?: Unit; "siUnit"?: SiUnit };

export type MeasurandDataT = { readonly "description": { readonly "en": "The metrology facts of a quantity kind — its unit, SI unit, and default accumulation. The kind's enum member carries the site-agnostic defaults; a `specification` composes measurand so a device/site can OVERRIDE them. The deterministic on-bus `slug` lives on the specification's `identity`, not here." }; readonly "prop": { readonly "accumulation": { readonly "schema": { readonly "enum": readonly ["cumulative", "cumulative_monotonic", "instantaneous"]; readonly "type": "string" } }; readonly "si_unit": Omit<SiUnitDataT, "schema" | "title"> & { readonly "schema": { readonly "type": "string" }; readonly "title": { readonly "en": "SI symbol" } }; readonly "unit": Omit<UnitDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Non-SI unit override of a published/stated value — stated only when the value isn't in its quantity kind's canonical SI/coherent unit (kW vs W, °C vs K). Omit ⇒ canonical SI unit."; readonly "pt": "Unidade não-SI de um valor publicado/declarado — declarada apenas quando o valor não está na unidade SI canônica do seu tipo de grandeza. Omitir ⇒ unidade SI canônica." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "Unit"; readonly "pt": "Unidade" } } }; readonly "title": { readonly "en": "Measurand" } };

const _data: MeasurandDataT = {
	"description": {
		"en": "The metrology facts of a quantity kind — its unit, SI unit, and default accumulation. The kind's enum member carries the site-agnostic defaults; a `specification` composes measurand so a device/site can OVERRIDE them. The deterministic on-bus `slug` lives on the specification's `identity`, not here."
	},
	"prop": {
		"accumulation": {
			"schema": {
				"enum": [
					"cumulative",
					"cumulative_monotonic",
					"instantaneous"
				],
				"type": "string"
			}
		},
		"si_unit": {
			...SiUnitData,
			"schema": {
				"type": "string"
			},
			"title": {
				"en": "SI symbol"
			}
		},
		"unit": {
			...UnitData,
			"description": {
				"en": "Non-SI unit override of a published/stated value — stated only when the value isn't in its quantity kind's canonical SI/coherent unit (kW vs W, °C vs K). Omit ⇒ canonical SI unit.",
				"pt": "Unidade não-SI de um valor publicado/declarado — declarada apenas quando o valor não está na unidade SI canônica do seu tipo de grandeza. Omitir ⇒ unidade SI canônica."
			},
			"schema": {
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "Unit",
				"pt": "Unidade"
			}
		}
	},
	"title": {
		"en": "Measurand"
	}
};
export default _data;
