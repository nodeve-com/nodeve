// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';
import * as archetypeSlots_ from '../property/archetype_slots.ts';
import * as featureSlots_ from '../property/feature_slots.ts';

export const schema: TSchema = Type.Object({ "featureSlots": Type.Optional(featureSlots_.schema), "archetypeSlots": Type.Optional(archetypeSlots_.schema) }, {"additionalProperties":false,"x-key-map":{"feature_slots":"featureSlots","archetype_slots":"archetypeSlots"}});

export type ArchetypeSettings = { "featureSlots"?: featureSlots_.FeatureSlots; "archetypeSlots"?: archetypeSlots_.ArchetypeSlots };

type DataT = { readonly "body": { readonly "en": "ASSEMBLY — the class-level half of the def language, the counterpart to features/concept_settings.yaml. `feature_slots` nests features; `archetype_slots` nests sibling classes as named slots. Both are `<slug>: overlay` maps where the key IS the slug (use-site renames don't exist — they'd break the name→def lookup); a `{ archetype: <slug> }` / `{ feature: <slug> }` overlay rebinds a slot's shape so the slot name may differ.\n\nWHY A SEPARATE BLOCK, not two more props on `concept_settings`: that block is composed by BOTH archetypes/feature.yaml and archetypes/archetype.yaml, so a slot added there is legal on a feature def too — and assembly is exactly what a feature must never do (a feature groups props and reaches a sibling's shape through `compose`, one layer, no nesting). Splitting the archetype-only verbs into their own feature makes \"archetype-level only\" true BY CONSTRUCTION: only archetypes/archetype.yaml declares this slot, so `feature_slots` on a feature def is an undeclared key and the closed projection rejects it. Convention enforced by a guard script is what this replaces — see the archetype.yaml body.\n\nInteriors are LEGALIZED, NOT VALIDATED: each map's keys must be slugs, its values are handed to kit/compile.ts. Resolving every slot against the concept its key names is the follow-up that closes them — the same def-language contract feature.yaml defers.\n" }; readonly "description": { readonly "en": "The def-language grammar only an ARCHETYPE may carry — the two assembly maps, kept out of the shared concept_settings block so a feature def can never reach them."; readonly "pt": "A gramática de def que só um ARQUÉTIPO pode transportar — os dois mapas de montagem, mantidos fora do bloco partilhado concept_settings para que uma def de elemento nunca lhes chegue." }; readonly "identity": { readonly "archetypeId": "feature"; readonly "slug": "archetype_settings" }; readonly "prop": { readonly "archetypeSlots": typeof archetypeSlots_; readonly "featureSlots": typeof featureSlots_ }; readonly "title": { readonly "en": "Archetype settings"; readonly "pt": "Configurações de arquétipo" } };

const _data: DataT = {
	"body": {
		"en": "ASSEMBLY — the class-level half of the def language, the counterpart to features/concept_settings.yaml. `feature_slots` nests features; `archetype_slots` nests sibling classes as named slots. Both are `<slug>: overlay` maps where the key IS the slug (use-site renames don't exist — they'd break the name→def lookup); a `{ archetype: <slug> }` / `{ feature: <slug> }` overlay rebinds a slot's shape so the slot name may differ.\n\nWHY A SEPARATE BLOCK, not two more props on `concept_settings`: that block is composed by BOTH archetypes/feature.yaml and archetypes/archetype.yaml, so a slot added there is legal on a feature def too — and assembly is exactly what a feature must never do (a feature groups props and reaches a sibling's shape through `compose`, one layer, no nesting). Splitting the archetype-only verbs into their own feature makes \"archetype-level only\" true BY CONSTRUCTION: only archetypes/archetype.yaml declares this slot, so `feature_slots` on a feature def is an undeclared key and the closed projection rejects it. Convention enforced by a guard script is what this replaces — see the archetype.yaml body.\n\nInteriors are LEGALIZED, NOT VALIDATED: each map's keys must be slugs, its values are handed to kit/compile.ts. Resolving every slot against the concept its key names is the follow-up that closes them — the same def-language contract feature.yaml defers.\n"
	},
	"description": {
		"en": "The def-language grammar only an ARCHETYPE may carry — the two assembly maps, kept out of the shared concept_settings block so a feature def can never reach them.",
		"pt": "A gramática de def que só um ARQUÉTIPO pode transportar — os dois mapas de montagem, mantidos fora do bloco partilhado concept_settings para que uma def de elemento nunca lhes chegue."
	},
	"identity": {
		"archetypeId": "feature",
		"slug": "archetype_settings"
	},
	"prop": {
		"archetypeSlots": archetypeSlots_,
		"featureSlots": featureSlots_
	},
	"title": {
		"en": "Archetype settings",
		"pt": "Configurações de arquétipo"
	}
};
export const { body, description, identity, prop, title } = _data;
