// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';
import * as bound_ from '../property/bound.ts';
import * as channel_ from '../property/channel.ts';
import * as direction_ from '../property/direction.ts';
import * as duration_ from '../property/duration.ts';
import * as fractionLower_ from '../property/fraction_lower.ts';
import * as fractionUpper_ from '../property/fraction_upper.ts';
import * as hysteresis_ from '../property/hysteresis.ts';
import * as margin_ from '../property/margin.ts';
import * as max_ from '../property/max.ts';
import * as maxPermissibleError_ from '../property/max_permissible_error.ts';
import * as min_ from '../property/min.ts';
import * as nominal_ from '../property/nominal.ts';
import * as nominalRange_ from './nominal_range.ts';
import * as resolution_ from '../property/resolution.ts';
import * as tolerance_ from '../property/tolerance.ts';
import * as unit_ from '../property/unit.ts';

export const schema: TSchema = Type.Object({ "min": Type.Optional(min_.schema), "max": Type.Optional(max_.schema), "nominal": Type.Optional(nominal_.schema), "fractionLower": Type.Optional(fractionLower_.schema), "fractionUpper": Type.Optional(fractionUpper_.schema), "intervalKind": Type.Optional(Type.Union([Type.Literal("measurable"), Type.Literal("rating"), Type.Literal("threshold"), Type.Literal("zone")], {"type":"string"})), "rating": Type.Optional(Type.Union([Type.Literal("continuous"), Type.Literal("intermittent"), Type.Literal("protection_required"), Type.Literal("short_term"), Type.Literal("shutdown"), Type.Literal("startup"), Type.Literal("survival")], {"type":"string"})), "zone": Type.Optional(Type.Union([Type.Literal("idle"), Type.Literal("mpp"), Type.Literal("mppt"), Type.Literal("off"), Type.Literal("open_circuit"), Type.Literal("running"), Type.Literal("short_circuit")], {"type":"string"})), "severity": Type.Optional(Type.Union([Type.Literal("best"), Type.Literal("better"), Type.Literal("caution"), Type.Literal("critical"), Type.Literal("fatal"), Type.Literal("good"), Type.Literal("notice"), Type.Literal("warning")], {"type":"string"})), "flowDirection": Type.Optional(Type.Union([Type.Literal("in"), Type.Literal("net"), Type.Literal("out")], {"type":"string"})), "period": Type.Optional(Type.Union([Type.Literal("daily"), Type.Literal("lifetime"), Type.Literal("monthly"), Type.Literal("yearly")], {"type":"string"})), "unit": Type.Optional(unit_.schema), "duration": Type.Optional(duration_.schema), "tolerance": Type.Optional(tolerance_.schema), "margin": Type.Optional(margin_.schema), "bound": Type.Optional(bound_.schema), "direction": Type.Optional(direction_.schema), "hysteresis": Type.Optional(hysteresis_.schema), "resolution": Type.Optional(resolution_.schema), "maxPermissibleError": Type.Optional(maxPermissibleError_.schema), "channel": Type.Optional(channel_.schema) }, {"additionalProperties":false,"x-key-map":{"fraction_lower":"fractionLower","fraction_upper":"fractionUpper","interval_kind":"intervalKind","flow_direction":"flowDirection","max_permissible_error":"maxPermissibleError"}});

export type Interval = { "min"?: min_.Min; "max"?: max_.Max; "nominal"?: nominal_.Nominal; "fractionLower"?: fractionLower_.FractionLower; "fractionUpper"?: fractionUpper_.FractionUpper; "intervalKind"?: "measurable" | "rating" | "threshold" | "zone"; "rating"?: "continuous" | "intermittent" | "protection_required" | "short_term" | "shutdown" | "startup" | "survival"; "zone"?: "idle" | "mpp" | "mppt" | "off" | "open_circuit" | "running" | "short_circuit"; "severity"?: "best" | "better" | "caution" | "critical" | "fatal" | "good" | "notice" | "warning"; "flowDirection"?: "in" | "net" | "out"; "period"?: "daily" | "lifetime" | "monthly" | "yearly"; "unit"?: unit_.Unit; "duration"?: duration_.Duration; "tolerance"?: tolerance_.Tolerance; "margin"?: margin_.Margin; "bound"?: bound_.Bound; "direction"?: direction_.Direction; "hysteresis"?: hysteresis_.Hysteresis; "resolution"?: resolution_.Resolution; "maxPermissibleError"?: maxPermissibleError_.MaxPermissibleError; "channel"?: channel_.Channel };

type DataT = { readonly "description": { readonly "en": "Rated/characterised regions: operating/survival/startup bands, MPPT window, conditional derates — the THING's own behaviour envelope." }; readonly "identity": { readonly "archetypeId": "feature"; readonly "slug": "interval" }; readonly "prop": { readonly "bound": typeof bound_; readonly "channel": typeof channel_; readonly "direction": typeof direction_; readonly "duration": typeof duration_; readonly "flowDirection": { readonly "description": { readonly "en": "Optional flow-channel identity: in / out / net. An IDENTITY axis, not a classifier — two intervals differing in flow_direction are SEPARATE channels (distinct registers, distinct sensor ids), not bands of one series. `in`/`out` are separate counters; `net` is one signed channel (the delta, sign = direction). Absent = undirected (voltage, temperature). Composes into the sensor id/key. Crosswalks to CIM FlowDirectionKind." }; readonly "title": { readonly "en": "Flow direction" } }; readonly "fractionLower": typeof fractionLower_; readonly "fractionUpper": typeof fractionUpper_; readonly "hysteresis": typeof hysteresis_; readonly "intervalKind": { readonly "description": { readonly "en": "What the interval IS. Three are STATELESS — `f(reading) → in-band?`, no history: `measurable` (instrument readable span), `rating` (a rated capability tier), `zone` (a named operating region → a boolean sensor). One is STATEFUL — `f(reading, prior_state) → on/off`: `threshold` (a hysteretic trigger: `bound` + `direction` + `hysteresis`, a startup/run-command/thermostat/hygrostat). DERIVED — never authored on the base axes — as `rating` from a rating tier, `zone` from a zone name, `threshold` from a `direction`; `measurable` is authored, since it isn't derivable from the bounds alone." }; readonly "title": { readonly "en": "Interval kind" } }; readonly "margin": typeof margin_; readonly "max": typeof max_; readonly "maxPermissibleError": typeof maxPermissibleError_; readonly "min": typeof min_; readonly "nominal": typeof nominal_; readonly "rating": { readonly "description": { readonly "en": "The rated tier of a `interval_kind: rating` band — how far / how long the quantity is rated: continuous (full capacity), intermittent / short_term (time-bounded, carry a duration), survival (endured), startup / shutdown (thresholds), protection_required (trip), nominal (a nameplate value, tolerance/margin allowed, no min/max). Closed and universal, so a consumer reasons about 'the continuous band' without archetype knowledge. Setting it derives interval_kind: rating." }; readonly "title": { readonly "en": "Rating" } }; readonly "resolution": typeof resolution_; readonly "severity": { readonly "description": { readonly "en": "Optional grade of a narrower sub-range within the base region, a bipolar ramp: desirable best → better → good, absent = nominal/no-concern, undesirable notice → caution → warning → critical → fatal. An IDENTITY axis: a `best` sub-band (a tight full-power MPPT window) inside a wider `good` one is its OWN addressable band + boolean sensor, keyed into the slug. The undesirable rungs crosswalk to syslog RFC 5424 severity and ISA-18.2 alarm priority; the desirable side has no standard equivalent." }; readonly "title": { readonly "en": "Severity" } }; readonly "tolerance": typeof tolerance_; readonly "unit": typeof unit_; readonly "zone": { readonly "description": { readonly "en": "The named operating region of an `interval_kind: zone` band — a region of THIS quantity's own axis: an electrical operating point/window (mppt, mpp, open_circuit, short_circuit) or a lifecycle status band (off, idle, running). Unlike a `rating` (the capability envelope), a zone captures where it ACTUALLY sits — a compressor's normal running draw vs its rated max. Each yields a boolean 'is the quantity in this region' sensor and may also anchor an interval_item. Setting it derives interval_kind: zone. Replaces the former free-form `mode`. Narrower sub-ranges grade via `severity`. A zone is STATELESS; for a stateful trigger (startup) use `interval_kind: threshold` below." }; readonly "title": { readonly "en": "Zone" } } }; readonly "title": { readonly "en": "Intervals"; readonly "pt": "Intervalos" } };

const _data: DataT = {
	"description": {
		"en": "Rated/characterised regions: operating/survival/startup bands, MPPT window, conditional derates — the THING's own behaviour envelope."
	},
	"identity": {
		"archetypeId": "feature",
		"slug": "interval"
	},
	"prop": {
		"bound": bound_,
		"channel": channel_,
		"direction": direction_,
		"duration": duration_,
		"flowDirection": {
			"description": {
				"en": "Optional flow-channel identity: in / out / net. An IDENTITY axis, not a classifier — two intervals differing in flow_direction are SEPARATE channels (distinct registers, distinct sensor ids), not bands of one series. `in`/`out` are separate counters; `net` is one signed channel (the delta, sign = direction). Absent = undirected (voltage, temperature). Composes into the sensor id/key. Crosswalks to CIM FlowDirectionKind."
			},
			"title": {
				"en": "Flow direction"
			}
		},
		"fractionLower": fractionLower_,
		"fractionUpper": fractionUpper_,
		"hysteresis": hysteresis_,
		"intervalKind": {
			"description": {
				"en": "What the interval IS. Three are STATELESS — `f(reading) → in-band?`, no history: `measurable` (instrument readable span), `rating` (a rated capability tier), `zone` (a named operating region → a boolean sensor). One is STATEFUL — `f(reading, prior_state) → on/off`: `threshold` (a hysteretic trigger: `bound` + `direction` + `hysteresis`, a startup/run-command/thermostat/hygrostat). DERIVED — never authored on the base axes — as `rating` from a rating tier, `zone` from a zone name, `threshold` from a `direction`; `measurable` is authored, since it isn't derivable from the bounds alone."
			},
			"title": {
				"en": "Interval kind"
			}
		},
		"margin": margin_,
		"max": max_,
		"maxPermissibleError": maxPermissibleError_,
		"min": min_,
		"nominal": nominal_,
		"rating": {
			"description": {
				"en": "The rated tier of a `interval_kind: rating` band — how far / how long the quantity is rated: continuous (full capacity), intermittent / short_term (time-bounded, carry a duration), survival (endured), startup / shutdown (thresholds), protection_required (trip), nominal (a nameplate value, tolerance/margin allowed, no min/max). Closed and universal, so a consumer reasons about 'the continuous band' without archetype knowledge. Setting it derives interval_kind: rating."
			},
			"title": {
				"en": "Rating"
			}
		},
		"resolution": resolution_,
		"severity": {
			"description": {
				"en": "Optional grade of a narrower sub-range within the base region, a bipolar ramp: desirable best → better → good, absent = nominal/no-concern, undesirable notice → caution → warning → critical → fatal. An IDENTITY axis: a `best` sub-band (a tight full-power MPPT window) inside a wider `good` one is its OWN addressable band + boolean sensor, keyed into the slug. The undesirable rungs crosswalk to syslog RFC 5424 severity and ISA-18.2 alarm priority; the desirable side has no standard equivalent."
			},
			"title": {
				"en": "Severity"
			}
		},
		"tolerance": tolerance_,
		"unit": unit_,
		"zone": {
			"description": {
				"en": "The named operating region of an `interval_kind: zone` band — a region of THIS quantity's own axis: an electrical operating point/window (mppt, mpp, open_circuit, short_circuit) or a lifecycle status band (off, idle, running). Unlike a `rating` (the capability envelope), a zone captures where it ACTUALLY sits — a compressor's normal running draw vs its rated max. Each yields a boolean 'is the quantity in this region' sensor and may also anchor an interval_item. Setting it derives interval_kind: zone. Replaces the former free-form `mode`. Narrower sub-ranges grade via `severity`. A zone is STATELESS; for a stateful trigger (startup) use `interval_kind: threshold` below."
			},
			"title": {
				"en": "Zone"
			}
		}
	},
	"title": {
		"en": "Intervals",
		"pt": "Intervalos"
	}
};
export const { description, identity, prop, title } = _data;
