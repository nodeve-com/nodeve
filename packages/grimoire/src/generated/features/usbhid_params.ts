// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import AccessData, { AccessSchema, type Access, type AccessDataT } from '../property/access.ts';
import ByteOrderData, { ByteOrderSchema, type ByteOrder, type ByteOrderDataT } from '../property/byte_order.ts';
import DecimalsData, { DecimalsSchema, type Decimals, type DecimalsDataT } from '../property/decimals.ts';
import IndexData, { IndexSchema, type Index, type IndexDataT } from '../property/index.ts';
import NameData, { NameSchema, type Name, type NameDataT } from '../property/name.ts';
import ScaleData, { ScaleSchema, type Scale, type ScaleDataT } from '../property/scale.ts';
import ScaleOverridesData, { ScaleOverridesSchema, type ScaleOverrides, type ScaleOverridesDataT } from '../property/scale_overrides.ts';
import SentinelData, { SentinelSchema, type Sentinel, type SentinelDataT } from '../property/sentinel.ts';
import Type_Data, { Type_Schema, type Type_, type Type_DataT } from '../property/type.ts';
import UnitData, { UnitSchema, type Unit, type UnitDataT } from '../property/unit.ts';

export const UsbhidParamsSchema: TSchema = Type.Object({ "type": Type.Optional(Type_Schema), "scale": Type.Optional(ScaleSchema), "decimals": Type.Optional(DecimalsSchema), "unit": Type.Optional(UnitSchema), "byte_order": Type.Optional(ByteOrderSchema), "scale_overrides": Type.Optional(ScaleOverridesSchema), "name": Type.Optional(NameSchema), "index": Type.Optional(IndexSchema), "access": Type.Optional(AccessSchema), "sentinel": Type.Optional(SentinelSchema) }, {"additionalProperties":false});

export type UsbhidParams = { "type"?: Type_; "scale"?: Scale; "decimals"?: Decimals; "unit"?: Unit; "byteOrder"?: ByteOrder; "scaleOverrides"?: ScaleOverrides; "name"?: Name; "index"?: Index; "access"?: Access; "sentinel"?: Sentinel };

export type UsbhidParamsDataT = { readonly "prop": { readonly "access": Omit<AccessDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Read, write-only (an action like factory RESET), or both. Most settings are read_write." }; readonly "schema": { readonly "enum": readonly ["read", "write", "read_write"]; readonly "type": "string" }; readonly "title": { readonly "en": "Access" } }; readonly "byte_order": Omit<ByteOrderDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Byte order for multi-byte types; omit ⇒ big_first (the network/HID default). Ignored for single-byte types." }; readonly "schema": { readonly "enum": readonly ["big_first", "little_first"]; readonly "type": "string" }; readonly "title": { readonly "en": "Byte order"; readonly "pt": "Ordem de bytes" } }; readonly "decimals": Omit<DecimalsDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Decimal places the sensor actually resolves; the published value is rounded to it. Source-side honesty — decoding leaks precision the sensor never measured (nearest float32 to 1.675 is 1.6749999523162842; a scale like 0.1 adds its own IEEE-754 tail). Omit only when scale already lands on the real resolution."; readonly "pt": "Casas decimais que o sensor realmente resolve; o valor publicado é arredondado para elas. Omita apenas quando a escala já chega à resolução real." }; readonly "schema": { readonly "minimum": 0; readonly "type": "integer" }; readonly "title": { readonly "en": "Resolved decimals"; readonly "pt": "Casas decimais resolvidas" } }; readonly "index": Omit<IndexDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "The field index the get/set command carries to address this parameter (m4ConfigField.index)." }; readonly "schema": { readonly "maximum": 255; readonly "minimum": 0; readonly "type": "integer" }; readonly "title": { readonly "en": "Index" } }; readonly "name": Omit<NameDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Machine identifier scoped INSIDE an enclosing concept's list (a config parameter, a tap window). Same nature as slug, but names one member within a thing rather than the whole thing."; readonly "pt": "Identificador de máquina no escopo DENTRO da lista de um conceito envolvente (um parâmetro de config, uma janela de tap). Mesma natureza que slug, mas nomeia um membro dentro de uma coisa, não a coisa inteira." }; readonly "schema": { readonly "pattern": "^[a-z][a-z0-9_]*$"; readonly "type": "string" }; readonly "title": { readonly "en": "Name"; readonly "pt": "Nome" } }; readonly "scale": Omit<ScaleDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Published value = raw × scale (raw → engineering units). Optional where omitting means 1 (VE.Direct)."; readonly "pt": "Valor publicado = bruto × escala (bruto → unidades de engenharia)." }; readonly "schema": { readonly "type": "number" }; readonly "title": { readonly "en": "Scale"; readonly "pt": "Escala" } }; readonly "scale_overrides": Omit<ScaleOverridesDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Per-firmware-profile scale overrides, keyed by the profile a link firmware rule selects. When the resolved profile has an entry, it replaces `scale`; otherwise `scale` stands." }; readonly "schema": { readonly "additionalProperties": { readonly "type": "number" }; readonly "propertyNames": { readonly "pattern": "^[a-z][a-z0-9_]*$" }; readonly "type": "object" }; readonly "title": { readonly "en": "Scale overrides"; readonly "pt": "Escalas por firmware" } }; readonly "sentinel": Omit<SentinelDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "A reserved raw value with a categorical meaning instead of a number (M4-ATX timers use 0xffff for never). raw == reading ⇒ emit the meaning slug rather than scaling; a writer maps back. The reserved reading is named raw, not value." }; readonly "schema": { readonly "additionalProperties": false; readonly "properties": { readonly "meaning": { readonly "pattern": "^[a-z][a-z0-9_]*$"; readonly "type": "string" }; readonly "raw": { readonly "minimum": 0; readonly "type": "integer" } }; readonly "required": readonly ["raw", "meaning"]; readonly "type": "object" }; readonly "title": { readonly "en": "Sentinel" } }; readonly "type": Omit<Type_DataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "On-the-wire encoding of the raw value (e.g. uint16, float32). Value set is medium-specific — modbus has no uint8; VE.Direct is int/hex/bool/string."; readonly "pt": "Codificação do valor bruto no barramento (ex. uint16, float32)." }; readonly "schema": { readonly "type": "string" }; readonly "title": { readonly "en": "Encoding"; readonly "pt": "Codificação" } }; readonly "unit": Omit<UnitDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Non-SI unit override of a published/stated value — stated only when the value isn't in its quantity kind's canonical SI/coherent unit (kW vs W, °C vs K). Omit ⇒ canonical SI unit."; readonly "pt": "Unidade não-SI de um valor publicado/declarado — declarada apenas quando o valor não está na unidade SI canônica do seu tipo de grandeza. Omitir ⇒ unidade SI canônica." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "Unit"; readonly "pt": "Unidade" } } } };

const _data: UsbhidParamsDataT = {
	"prop": {
		"access": {
			...AccessData,
			"description": {
				"en": "Read, write-only (an action like factory RESET), or both. Most settings are read_write."
			},
			"schema": {
				"enum": [
					"read",
					"write",
					"read_write"
				],
				"type": "string"
			},
			"title": {
				"en": "Access"
			}
		},
		"byte_order": {
			...ByteOrderData,
			"description": {
				"en": "Byte order for multi-byte types; omit ⇒ big_first (the network/HID default). Ignored for single-byte types."
			},
			"schema": {
				"enum": [
					"big_first",
					"little_first"
				],
				"type": "string"
			},
			"title": {
				"en": "Byte order",
				"pt": "Ordem de bytes"
			}
		},
		"decimals": {
			...DecimalsData,
			"description": {
				"en": "Decimal places the sensor actually resolves; the published value is rounded to it. Source-side honesty — decoding leaks precision the sensor never measured (nearest float32 to 1.675 is 1.6749999523162842; a scale like 0.1 adds its own IEEE-754 tail). Omit only when scale already lands on the real resolution.",
				"pt": "Casas decimais que o sensor realmente resolve; o valor publicado é arredondado para elas. Omita apenas quando a escala já chega à resolução real."
			},
			"schema": {
				"minimum": 0,
				"type": "integer"
			},
			"title": {
				"en": "Resolved decimals",
				"pt": "Casas decimais resolvidas"
			}
		},
		"index": {
			...IndexData,
			"description": {
				"en": "The field index the get/set command carries to address this parameter (m4ConfigField.index)."
			},
			"schema": {
				"maximum": 255,
				"minimum": 0,
				"type": "integer"
			},
			"title": {
				"en": "Index"
			}
		},
		"name": {
			...NameData,
			"description": {
				"en": "Machine identifier scoped INSIDE an enclosing concept's list (a config parameter, a tap window). Same nature as slug, but names one member within a thing rather than the whole thing.",
				"pt": "Identificador de máquina no escopo DENTRO da lista de um conceito envolvente (um parâmetro de config, uma janela de tap). Mesma natureza que slug, mas nomeia um membro dentro de uma coisa, não a coisa inteira."
			},
			"schema": {
				"pattern": "^[a-z][a-z0-9_]*$",
				"type": "string"
			},
			"title": {
				"en": "Name",
				"pt": "Nome"
			}
		},
		"scale": {
			...ScaleData,
			"description": {
				"en": "Published value = raw × scale (raw → engineering units). Optional where omitting means 1 (VE.Direct).",
				"pt": "Valor publicado = bruto × escala (bruto → unidades de engenharia)."
			},
			"schema": {
				"type": "number"
			},
			"title": {
				"en": "Scale",
				"pt": "Escala"
			}
		},
		"scale_overrides": {
			...ScaleOverridesData,
			"description": {
				"en": "Per-firmware-profile scale overrides, keyed by the profile a link firmware rule selects. When the resolved profile has an entry, it replaces `scale`; otherwise `scale` stands."
			},
			"schema": {
				"additionalProperties": {
					"type": "number"
				},
				"propertyNames": {
					"pattern": "^[a-z][a-z0-9_]*$"
				},
				"type": "object"
			},
			"title": {
				"en": "Scale overrides",
				"pt": "Escalas por firmware"
			}
		},
		"sentinel": {
			...SentinelData,
			"description": {
				"en": "A reserved raw value with a categorical meaning instead of a number (M4-ATX timers use 0xffff for never). raw == reading ⇒ emit the meaning slug rather than scaling; a writer maps back. The reserved reading is named raw, not value."
			},
			"schema": {
				"additionalProperties": false,
				"properties": {
					"meaning": {
						"pattern": "^[a-z][a-z0-9_]*$",
						"type": "string"
					},
					"raw": {
						"minimum": 0,
						"type": "integer"
					}
				},
				"required": [
					"raw",
					"meaning"
				],
				"type": "object"
			},
			"title": {
				"en": "Sentinel"
			}
		},
		"type": {
			...Type_Data,
			"description": {
				"en": "On-the-wire encoding of the raw value (e.g. uint16, float32). Value set is medium-specific — modbus has no uint8; VE.Direct is int/hex/bool/string.",
				"pt": "Codificação do valor bruto no barramento (ex. uint16, float32)."
			},
			"schema": {
				"type": "string"
			},
			"title": {
				"en": "Encoding",
				"pt": "Codificação"
			}
		},
		"unit": {
			...UnitData,
			"description": {
				"en": "Non-SI unit override of a published/stated value — stated only when the value isn't in its quantity kind's canonical SI/coherent unit (kW vs W, °C vs K). Omit ⇒ canonical SI unit.",
				"pt": "Unidade não-SI de um valor publicado/declarado — declarada apenas quando o valor não está na unidade SI canônica do seu tipo de grandeza. Omitir ⇒ unidade SI canônica."
			},
			"schema": {
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "Unit",
				"pt": "Unidade"
			}
		}
	}
};
export default _data;
