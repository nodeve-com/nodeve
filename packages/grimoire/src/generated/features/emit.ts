// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import FormatData, { FormatSchema, type Format, type FormatDataT } from '../property/format.ts';
import PublishData, { PublishSchema, type Publish, type PublishDataT } from '../property/publish.ts';
import TopicPrefixData, { TopicPrefixSchema, type TopicPrefix, type TopicPrefixDataT } from '../property/topic_prefix.ts';
import UpdateIntervalMsData, { UpdateIntervalMsSchema, type UpdateIntervalMs, type UpdateIntervalMsDataT } from '../property/update_interval_ms.ts';

export const EmitSchema: TSchema = Type.Object({ "topic_prefix": Type.Optional(TopicPrefixSchema), "update_interval_ms": Type.Optional(UpdateIntervalMsSchema), "publish": Type.Optional(PublishSchema), "format": Type.Optional(FormatSchema) }, {"additionalProperties":false});

export type Emit = { "topicPrefix"?: TopicPrefix; "updateIntervalMs"?: UpdateIntervalMs; "publish"?: Publish; "format"?: Format };

export type EmitDataT = { readonly "prop": { readonly "format": Omit<FormatDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Wire layout for decoded points. grouped (default when absent): one retained JSON object per group at <topic_prefix>/<group> = {ts, mono, seq, ...values}. per_value: one retained bare-value topic per point at <topic_prefix>/<group>/<key> (no envelope) — the flat layout legacy per-value subscribers expect. Medium-neutral so any decoder-to-MQTT medium can offer either contract." }; readonly "schema": { readonly "enum": readonly ["grouped", "per_value"]; readonly "type": "string" }; readonly "title": { readonly "en": "Wire format" } }; readonly "publish": Omit<PublishDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "When to publish: on change only, or every sample." }; readonly "schema": { readonly "enum": readonly ["changed", "always"]; readonly "type": "string" }; readonly "title": { readonly "en": "Publish policy" } }; readonly "topic_prefix": Omit<TopicPrefixDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "MQTT topic root a decoded value is published under." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "Topic prefix" } }; readonly "update_interval_ms": Omit<UpdateIntervalMsDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Cadence at which a fresh point is expected/emitted, integer milliseconds (industrial Modbus-master convention). Named for the RATE, not a fetch strategy — a polling driver reads at this interval, a push/discovered driver throttles to it. Required in the catalog emit contract; optional where a downstream consumer overrides the catalog default." }; readonly "schema": { readonly "exclusiveMinimum": 0; readonly "type": "integer" }; readonly "title": { readonly "en": "Update interval (ms)" } } } };

const _data: EmitDataT = {
	"prop": {
		"format": {
			...FormatData,
			"description": {
				"en": "Wire layout for decoded points. grouped (default when absent): one retained JSON object per group at <topic_prefix>/<group> = {ts, mono, seq, ...values}. per_value: one retained bare-value topic per point at <topic_prefix>/<group>/<key> (no envelope) — the flat layout legacy per-value subscribers expect. Medium-neutral so any decoder-to-MQTT medium can offer either contract."
			},
			"schema": {
				"enum": [
					"grouped",
					"per_value"
				],
				"type": "string"
			},
			"title": {
				"en": "Wire format"
			}
		},
		"publish": {
			...PublishData,
			"description": {
				"en": "When to publish: on change only, or every sample."
			},
			"schema": {
				"enum": [
					"changed",
					"always"
				],
				"type": "string"
			},
			"title": {
				"en": "Publish policy"
			}
		},
		"topic_prefix": {
			...TopicPrefixData,
			"description": {
				"en": "MQTT topic root a decoded value is published under."
			},
			"schema": {
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "Topic prefix"
			}
		},
		"update_interval_ms": {
			...UpdateIntervalMsData,
			"description": {
				"en": "Cadence at which a fresh point is expected/emitted, integer milliseconds (industrial Modbus-master convention). Named for the RATE, not a fetch strategy — a polling driver reads at this interval, a push/discovered driver throttles to it. Required in the catalog emit contract; optional where a downstream consumer overrides the catalog default."
			},
			"schema": {
				"exclusiveMinimum": 0,
				"type": "integer"
			},
			"title": {
				"en": "Update interval (ms)"
			}
		}
	}
};
export default _data;
