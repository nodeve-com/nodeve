// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import FqdnData, { FqdnSchema, type Fqdn, type FqdnDataT } from '../property/fqdn.ts';
import IpAddressData, { IpAddressSchema, type IpAddress, type IpAddressDataT } from '../property/ip_address.ts';

export const IpBindingSchema: TSchema = Type.Object({ "ip_family": Type.Optional(Type.Union([Type.Literal("ipv4"), Type.Literal("ipv6")], {"type":"string"})), "ip_address": Type.Optional(IpAddressSchema), "fqdn": Type.Optional(FqdnSchema) }, {"additionalProperties":false});

export type IpBinding = { "ipFamily"?: "ipv4" | "ipv6"; "ipAddress"?: IpAddress; "fqdn"?: Fqdn };

export type IpBindingDataT = { readonly "prop": { readonly "fqdn": Omit<FqdnDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Fully qualified domain name resolving to this address (host + domain, e.g. inverter.lan). No trailing dot. Optional — an address need not have a name." }; readonly "schema": { readonly "maxLength": 253; readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "FQDN" } }; readonly "ip_address": Omit<IpAddressDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "An L3 address assigned to the interface — an IPv4 dotted-quad (192.0.2.10) or IPv6 (2001:db8::1). The `ip_family` sibling records which. No sensible default — deployment-specific, supplied downstream." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "IP address" } }; readonly "ip_family": { readonly "schema": { readonly "enum": readonly ["ipv4", "ipv6"]; readonly "type": "string" } } }; readonly "title": { readonly "en": "IP binding" } };

const _data: IpBindingDataT = {
	"prop": {
		"fqdn": {
			...FqdnData,
			"description": {
				"en": "Fully qualified domain name resolving to this address (host + domain, e.g. inverter.lan). No trailing dot. Optional — an address need not have a name."
			},
			"schema": {
				"maxLength": 253,
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "FQDN"
			}
		},
		"ip_address": {
			...IpAddressData,
			"description": {
				"en": "An L3 address assigned to the interface — an IPv4 dotted-quad (192.0.2.10) or IPv6 (2001:db8::1). The `ip_family` sibling records which. No sensible default — deployment-specific, supplied downstream."
			},
			"schema": {
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "IP address"
			}
		},
		"ip_family": {
			"schema": {
				"enum": [
					"ipv4",
					"ipv6"
				],
				"type": "string"
			}
		}
	},
	"title": {
		"en": "IP binding"
	}
};
export default _data;
