// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import ExpectData, { ExpectSchema, type Expect, type ExpectDataT } from '../property/expect.ts';
import OffsetData, { OffsetSchema, type Offset, type OffsetDataT } from '../property/offset.ts';
import ResponseMagicData, { ResponseMagicSchema, type ResponseMagic, type ResponseMagicDataT } from '../property/response_magic.ts';

export const MagicByteSchema: TSchema = Type.Object({ "offset": Type.Optional(OffsetSchema), "expect": Type.Optional(ExpectSchema), "response_magic": Type.Optional(ResponseMagicSchema) }, {"additionalProperties":false});

export type MagicByte = { "offset"?: Offset; "expect"?: Expect; "responseMagic"?: ResponseMagic };

export type MagicByteDataT = { readonly "prop": { readonly "expect": Omit<ExpectDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "The byte expected at offset in the frame (reject the frame otherwise). Named expect, not value — it's the byte the frame must carry, distinct from the many other value concepts." }; readonly "schema": { readonly "maximum": 255; readonly "minimum": 0; readonly "type": "integer" }; readonly "title": { readonly "en": "Expected byte" } }; readonly "offset": Omit<OffsetDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Zero-based byte offset into a request/response frame. The single place the key is defined — diag byte-fields, magic-byte checks and firmware-version bytes all compose it." }; readonly "schema": { readonly "minimum": 0; readonly "type": "integer" }; readonly "title": { readonly "en": "Byte offset" } }; readonly "response_magic": Omit<ResponseMagicDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "A leading sanity byte-check a device stamps on a valid frame/response, as { offset, expect }. Shared by the usbhid diag read and config exchange." }; readonly "schema": { readonly "type": "object" }; readonly "title": { readonly "en": "Response magic" } } } };

const _data: MagicByteDataT = {
	"prop": {
		"expect": {
			...ExpectData,
			"description": {
				"en": "The byte expected at offset in the frame (reject the frame otherwise). Named expect, not value — it's the byte the frame must carry, distinct from the many other value concepts."
			},
			"schema": {
				"maximum": 255,
				"minimum": 0,
				"type": "integer"
			},
			"title": {
				"en": "Expected byte"
			}
		},
		"offset": {
			...OffsetData,
			"description": {
				"en": "Zero-based byte offset into a request/response frame. The single place the key is defined — diag byte-fields, magic-byte checks and firmware-version bytes all compose it."
			},
			"schema": {
				"minimum": 0,
				"type": "integer"
			},
			"title": {
				"en": "Byte offset"
			}
		},
		"response_magic": {
			...ResponseMagicData,
			"description": {
				"en": "A leading sanity byte-check a device stamps on a valid frame/response, as { offset, expect }. Shared by the usbhid diag read and config exchange."
			},
			"schema": {
				"type": "object"
			},
			"title": {
				"en": "Response magic"
			}
		}
	}
};
export default _data;
