// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import PathData, { PathSchema, type Path, type PathDataT } from '../property/path.ts';

export const UnixSocketSchema: TSchema = Type.Object({ "path": Type.Optional(PathSchema) }, {"additionalProperties":false});

export type UnixSocket = { "path"?: Path };

export type UnixSocketDataT = { readonly "prop": { readonly "path": Omit<PathDataT, "description" | "refs" | "schema" | "title"> & { readonly "description": { readonly "en": "Filesystem path of a Unix-domain (AF_UNIX) socket — same string whether a process binds or connects." }; readonly "refs": readonly [{ readonly "match": "exact"; readonly "registry_id": "posix"; readonly "term": "AF_UNIX" }]; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "Path"; readonly "pt": "Caminho" } } } };

const _data: UnixSocketDataT = {
	"prop": {
		"path": {
			...PathData,
			"description": {
				"en": "Filesystem path of a Unix-domain (AF_UNIX) socket — same string whether a process binds or connects."
			},
			"refs": [
				{
					"match": "exact",
					"registry_id": "posix",
					"term": "AF_UNIX"
				}
			],
			"schema": {
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "Path",
				"pt": "Caminho"
			}
		}
	}
};
export default _data;
