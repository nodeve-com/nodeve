// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import BindData, { BindSchema, type Bind, type BindDataT } from '../property/bind.ts';
import IpAddressIdData, { IpAddressIdSchema, type IpAddressId, type IpAddressIdDataT } from '../property/ip_address_id.ts';
import NetworkInterfaceIdData, { NetworkInterfaceIdSchema, type NetworkInterfaceId, type NetworkInterfaceIdDataT } from '../property/network_interface_id.ts';
import PortData, { PortSchema, type Port, type PortDataT } from '../property/port.ts';

export const ServiceBindingSchema: TSchema = Type.Object({ "service_protocol": Type.Optional(Type.Union([Type.Literal("http"), Type.Literal("https"), Type.Literal("modbus_tcp"), Type.Literal("mqtt"), Type.Literal("mqtts"), Type.Literal("ws"), Type.Literal("wss")], {"type":"string"})), "network_interface_id": Type.Optional(NetworkInterfaceIdSchema), "ip_address_id": Type.Optional(IpAddressIdSchema), "port": Type.Optional(PortSchema), "bind": Type.Optional(BindSchema) }, {"additionalProperties":false,"not":{"required":["network_interface_id","ip_address_id"]}});

export type ServiceBinding = { "serviceProtocol"?: "http" | "https" | "modbus_tcp" | "mqtt" | "mqtts" | "ws" | "wss"; "networkInterfaceId"?: NetworkInterfaceId; "ipAddressId"?: IpAddressId; "port"?: Port; "bind"?: Bind };

export type ServiceBindingDataT = { readonly "description": { readonly "en": "The local listen socket of a service — the protocol it speaks (service_protocol) and which of the node's interfaces/addresses it binds to (by reference into the node's own network_interfaces/ip_addresses), on which port. The serve-side mirror of endpoint." }; readonly "prop": { readonly "bind": Omit<BindDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Local address a listener binds to (e.g. 0.0.0.0, 127.0.0.1). Mirror of host (remote/dial side). No default — 0.0.0.0 exposes the listener to the whole network, a real security boundary, so the choice is authored explicitly, never inherited." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "Bind address" } }; readonly "ip_address_id": Omit<IpAddressIdDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Slug of an ip_addresses instance on this node the service listens on (the runtime resolves it against the node's own addresses). A slug value but a distinct key — it names a relationship, not this object's own id." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "IP address" } }; readonly "network_interface_id": Omit<NetworkInterfaceIdDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Slug of a network_interfaces instance on this node the service binds to (the runtime resolves it against the node's own interfaces). A slug value but a distinct key — it names a relationship, not this object's own id." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "Network interface" } }; readonly "port": Omit<PortDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "TCP/UDP port number. Omit to fall back to the scheme's well-known default." }; readonly "schema": { readonly "maximum": 65535; readonly "minimum": 1; readonly "type": "integer" }; readonly "title": { readonly "en": "Port" } }; readonly "service_protocol": { readonly "schema": { readonly "enum": readonly ["http", "https", "modbus_tcp", "mqtt", "mqtts", "ws", "wss"]; readonly "type": "string" } } }; readonly "schema": { readonly "not": { readonly "required": readonly ["network_interface_id", "ip_address_id"] } }; readonly "title": { readonly "en": "Service binding" } };

const _data: ServiceBindingDataT = {
	"description": {
		"en": "The local listen socket of a service — the protocol it speaks (service_protocol) and which of the node's interfaces/addresses it binds to (by reference into the node's own network_interfaces/ip_addresses), on which port. The serve-side mirror of endpoint."
	},
	"prop": {
		"bind": {
			...BindData,
			"description": {
				"en": "Local address a listener binds to (e.g. 0.0.0.0, 127.0.0.1). Mirror of host (remote/dial side). No default — 0.0.0.0 exposes the listener to the whole network, a real security boundary, so the choice is authored explicitly, never inherited."
			},
			"schema": {
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "Bind address"
			}
		},
		"ip_address_id": {
			...IpAddressIdData,
			"description": {
				"en": "Slug of an ip_addresses instance on this node the service listens on (the runtime resolves it against the node's own addresses). A slug value but a distinct key — it names a relationship, not this object's own id."
			},
			"schema": {
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "IP address"
			}
		},
		"network_interface_id": {
			...NetworkInterfaceIdData,
			"description": {
				"en": "Slug of a network_interfaces instance on this node the service binds to (the runtime resolves it against the node's own interfaces). A slug value but a distinct key — it names a relationship, not this object's own id."
			},
			"schema": {
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "Network interface"
			}
		},
		"port": {
			...PortData,
			"description": {
				"en": "TCP/UDP port number. Omit to fall back to the scheme's well-known default."
			},
			"schema": {
				"maximum": 65535,
				"minimum": 1,
				"type": "integer"
			},
			"title": {
				"en": "Port"
			}
		},
		"service_protocol": {
			"schema": {
				"enum": [
					"http",
					"https",
					"modbus_tcp",
					"mqtt",
					"mqtts",
					"ws",
					"wss"
				],
				"type": "string"
			}
		}
	},
	"schema": {
		"not": {
			"required": [
				"network_interface_id",
				"ip_address_id"
			]
		}
	},
	"title": {
		"en": "Service binding"
	}
};
export default _data;
