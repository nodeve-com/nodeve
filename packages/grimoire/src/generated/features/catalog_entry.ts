// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import AliasesData, { AliasesSchema, type Aliases, type AliasesDataT } from '../property/aliases.ts';
import IdData, { IdSchema, type Id, type IdDataT } from '../property/id.ts';

export const CatalogEntrySchema: TSchema = Type.Object({ "id": Type.Optional(IdSchema), "aliases": Type.Optional(AliasesSchema) }, {"additionalProperties":false});

export type CatalogEntry = { "id"?: Id; "aliases"?: Aliases };

export type CatalogEntryDataT = { readonly "prop": { readonly "aliases": Omit<AliasesDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Former tree paths this catalog leaf moved from (the inline changelog). loadDevice resolves them with a deprecation warning. Authored in the leaf YAML; omitted when empty." }; readonly "schema": { readonly "items": { readonly "minLength": 1; readonly "type": "string" }; readonly "type": "array" }; readonly "title": { readonly "en": "Aliases" } }; readonly "id": Omit<IdDataT, "schema" | "title"> & { readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "UUID" } } } };

const _data: CatalogEntryDataT = {
	"prop": {
		"aliases": {
			...AliasesData,
			"description": {
				"en": "Former tree paths this catalog leaf moved from (the inline changelog). loadDevice resolves them with a deprecation warning. Authored in the leaf YAML; omitted when empty."
			},
			"schema": {
				"items": {
					"minLength": 1,
					"type": "string"
				},
				"type": "array"
			},
			"title": {
				"en": "Aliases"
			}
		},
		"id": {
			...IdData,
			"schema": {
				"minLength": 1,
				"type": "string"
			},
			"title": {
				"en": "UUID"
			}
		}
	}
};
export default _data;
