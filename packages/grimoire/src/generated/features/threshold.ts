// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import BoundData, { BoundSchema, type Bound, type BoundDataT } from '../property/bound.ts';
import DirectionData, { DirectionSchema, type Direction, type DirectionDataT } from '../property/direction.ts';
import HysteresisData, { HysteresisSchema, type Hysteresis, type HysteresisDataT } from '../property/hysteresis.ts';

export const ThresholdSchema: TSchema = Type.Object({ "bound": Type.Optional(BoundSchema), "direction": Type.Optional(DirectionSchema), "hysteresis": Type.Optional(HysteresisSchema) }, {"additionalProperties":false});

export type Threshold = { "bound"?: Bound; "direction"?: Direction; "hysteresis"?: Hysteresis };

export type ThresholdDataT = { readonly "prop": { readonly "bound": Omit<BoundDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "The single bound a value is tested against (a threshold boundary), in the enclosing concept's unit. Named `bound`, not `value` — `value` is reserved for an actual magnitude." }; readonly "schema": { readonly "type": "number" }; readonly "title": { readonly "en": "Bound"; readonly "pt": "Limite" } }; readonly "direction": Omit<DirectionDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Which side of the threshold trips: above ⇒ trips when the reading exceeds `at`; below ⇒ when it drops under. Borrowed from HA numeric_state (above/below)." }; readonly "schema": { readonly "enum": readonly ["above", "below"]; readonly "type": "string" }; readonly "title": { readonly "en": "Direction"; readonly "pt": "Direção" } }; readonly "hysteresis": Omit<HysteresisDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Optional deadband around `at` to suppress chatter — once tripped, the reading must travel this far back past `at` to clear. Same unit as `at`; never fabricate." }; readonly "schema": { readonly "exclusiveMinimum": 0; readonly "type": "number" }; readonly "title": { readonly "en": "Hysteresis"; readonly "pt": "Histerese" } } } };

const _data: ThresholdDataT = {
	"prop": {
		"bound": {
			...BoundData,
			"description": {
				"en": "The single bound a value is tested against (a threshold boundary), in the enclosing concept's unit. Named `bound`, not `value` — `value` is reserved for an actual magnitude."
			},
			"schema": {
				"type": "number"
			},
			"title": {
				"en": "Bound",
				"pt": "Limite"
			}
		},
		"direction": {
			...DirectionData,
			"description": {
				"en": "Which side of the threshold trips: above ⇒ trips when the reading exceeds `at`; below ⇒ when it drops under. Borrowed from HA numeric_state (above/below)."
			},
			"schema": {
				"enum": [
					"above",
					"below"
				],
				"type": "string"
			},
			"title": {
				"en": "Direction",
				"pt": "Direção"
			}
		},
		"hysteresis": {
			...HysteresisData,
			"description": {
				"en": "Optional deadband around `at` to suppress chatter — once tripped, the reading must travel this far back past `at` to clear. Same unit as `at`; never fabricate."
			},
			"schema": {
				"exclusiveMinimum": 0,
				"type": "number"
			},
			"title": {
				"en": "Hysteresis",
				"pt": "Histerese"
			}
		}
	}
};
export default _data;
