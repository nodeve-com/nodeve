// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';
import * as byteOrder_ from '../property/byte_order.ts';
import * as decimals_ from '../property/decimals.ts';
import * as intervalItem_ from '../property/interval_item.ts';
import * as offset_ from '../property/offset.ts';
import * as scale_ from '../property/scale.ts';
import * as scaleOverrides_ from '../property/scale_overrides.ts';
import * as type_ from '../property/type.ts';
import * as unit_ from '../property/unit.ts';

export const schema: TSchema = Type.Object({ "intervalItem": Type.Optional(intervalItem_.schema), "type": Type.Optional(type_.schema), "scale": Type.Optional(scale_.schema), "decimals": Type.Optional(decimals_.schema), "unit": Type.Optional(unit_.schema), "byteOrder": Type.Optional(byteOrder_.schema), "scaleOverrides": Type.Optional(scaleOverrides_.schema), "offset": Type.Optional(offset_.schema) }, {"additionalProperties":false,"x-key-map":{"interval_item":"intervalItem","byte_order":"byteOrder","scale_overrides":"scaleOverrides"}});

export type UsbhidField = { "intervalItem"?: intervalItem_.IntervalItem; "type"?: type_.Type_; "scale"?: scale_.Scale; "decimals"?: decimals_.Decimals; "unit"?: unit_.Unit; "byteOrder"?: byteOrder_.ByteOrder; "scaleOverrides"?: scaleOverrides_.ScaleOverrides; "offset"?: offset_.Offset };

type DataT = { readonly "body": { readonly "en": "The USB-HID byte-field map — each value the poll response carries, at a byte OFFSET into the frame, scaled to an engineering NUMBER, linked to the measurand it reads. The USB-HID analogue of modbus_registers: same measurand-link + scale + topic-derivation model, addressed by byte offset into a HID report instead of a 16-bit register address. Encoding `type` values: uint8/int8/ uint16/int16/uint32/int32/float32 (modbus's set extended down to single bytes — a HID report is byte-addressed). `scale` is the DEFAULT-firmware value; older firmwares override via scale_overrides (usbhid_numeric).\n" }; readonly "identity": { readonly "archetypeId": "feature"; readonly "slug": "usbhid_field" }; readonly "prop": { readonly "byteOrder": typeof byteOrder_; readonly "decimals": typeof decimals_; readonly "intervalItem": typeof intervalItem_; readonly "offset": typeof offset_; readonly "scale": typeof scale_; readonly "scaleOverrides": typeof scaleOverrides_; readonly "type": typeof type_; readonly "unit": typeof unit_ } };

const _data: DataT = {
	"body": {
		"en": "The USB-HID byte-field map — each value the poll response carries, at a byte OFFSET into the frame, scaled to an engineering NUMBER, linked to the measurand it reads. The USB-HID analogue of modbus_registers: same measurand-link + scale + topic-derivation model, addressed by byte offset into a HID report instead of a 16-bit register address. Encoding `type` values: uint8/int8/ uint16/int16/uint32/int32/float32 (modbus's set extended down to single bytes — a HID report is byte-addressed). `scale` is the DEFAULT-firmware value; older firmwares override via scale_overrides (usbhid_numeric).\n"
	},
	"identity": {
		"archetypeId": "feature",
		"slug": "usbhid_field"
	},
	"prop": {
		"byteOrder": byteOrder_,
		"decimals": decimals_,
		"intervalItem": intervalItem_,
		"offset": offset_,
		"scale": scale_,
		"scaleOverrides": scaleOverrides_,
		"type": type_,
		"unit": unit_
	}
};
export const { body, identity, prop } = _data;
