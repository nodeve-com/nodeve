// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import ComposeData, { ComposeSchema, type Compose, type ComposeDataT } from '../property/compose.ts';
import CountData, { CountSchema, type Count, type CountDataT } from '../property/count.ts';
import IsArrayData, { IsArraySchema, type IsArray, type IsArrayDataT } from '../property/is_array.ts';
import IsSpecificationData, { IsSpecificationSchema, type IsSpecification, type IsSpecificationDataT } from '../property/is_specification.ts';
import MapData, { MapSchema, type Map, type MapDataT } from '../property/map.ts';
import PartData, { PartSchema, type Part, type PartDataT } from '../property/part.ts';
import RepeatedData, { RepeatedSchema, type Repeated, type RepeatedDataT } from '../property/repeated.ts';

export const ConceptSettingsSchema: TSchema = Type.Object({ "compose": Type.Optional(ComposeSchema), "repeated": Type.Optional(RepeatedSchema), "part": Type.Optional(PartSchema), "is_array": Type.Optional(IsArraySchema), "map": Type.Optional(MapSchema), "count": Type.Optional(CountSchema), "is_specification": Type.Optional(IsSpecificationSchema) }, {"additionalProperties":false});

export type ConceptSettings = { "compose"?: Compose; "repeated"?: Repeated; "part"?: Part; "isArray"?: IsArray; "map"?: Map; "count"?: Count; "isSpecification"?: IsSpecification };

export type ConceptSettingsDataT = { readonly "prop": { readonly "compose": Omit<ComposeDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "FLATTEN the named shapes' props into this def — a single slug or a list. The old `alias` (a same-shape rename) is just a one-element compose. Each target must be an object shape; a def groups props, never IS a scalar." }; readonly "schema": { readonly "anyOf": readonly [{ readonly "pattern": "^[a-z][a-z0-9_]*$"; readonly "type": "string" }, { readonly "items": { readonly "pattern": "^[a-z][a-z0-9_]*$"; readonly "type": "string" }; readonly "minItems": 1; readonly "type": "array" }] }; readonly "title": { readonly "en": "Compose"; readonly "pt": "Compor" } }; readonly "count": Omit<CountDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Number of discrete instances of a repeated feature (1 is legal: one discrete circuit). Its PRESENCE is the explicit nature tag: present = discrete countable instance(s); absent = inherently-singular aggregate. Never count: 1 for an aggregate; never omit for something you could see two of." }; readonly "schema": { readonly "minimum": 1; readonly "type": "integer" }; readonly "title": { readonly "en": "Count" } }; readonly "is_array": Omit<IsArrayDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "The feature's intrinsic cardinality: true = the value IS a bare LIST of its shape, `[shape, shape, …]`, with nothing wrapping it (refs rows, register maps, per-string install facts). Every member is enumerated; there is no count or aggregate envelope — that is `repeated`. Plural slug = array, per the naming rule. Exclusive with `map` (a slug-keyed record) and with `repeated` (the counted-instance envelope)." }; readonly "schema": { readonly "type": "boolean" }; readonly "title": { readonly "en": "Is array"; readonly "pt": "É lista" } }; readonly "is_specification": Omit<IsSpecificationDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Flag: true marks a feature as a SPECIFICATION carrier — each quantity_kind field becomes a `specification` (identity + measurand + `intervals`, the one list of rated bands) — regions covering both the thing's own behaviour envelope) AND the sensor's readable span (a `rating: measurable` interval), told apart by axis, not split into separate slots. The first-class replacement for the quantity_kind→spec binding: a spec-carrying feature declares this instead of relying on its props happening to be quantity_kinds. Independent of cardinality (array/map/repeated): a specification feature may itself be a list/map of spec-carrying shapes." }; readonly "schema": { readonly "type": "boolean" }; readonly "title": { readonly "en": "Is specification"; readonly "pt": "É especificação" } }; readonly "map": Omit<MapDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "The feature's intrinsic cardinality: true = the feature is a slug-keyed RECORD of its shape (vedirect_fields). Exclusive with array." }; readonly "schema": { readonly "type": "boolean" }; readonly "title": { readonly "en": "Map"; readonly "pt": "Mapa" } }; readonly "part": Omit<PartDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "The def-language fixed-parts expansion: names a parts/<slug>.yaml map (e.g. electrical_ac_phase_three) whose kinds become the feature's {combined, default, part} breakdown. Exclusive with `repeated` (a counted instance set)." }; readonly "schema": { readonly "pattern": "^[a-z][a-z0-9_]*$"; readonly "type": "string" }; readonly "title": { readonly "en": "Part"; readonly "pt": "Parte" } }; readonly "repeated": Omit<RepeatedDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "The feature's intrinsic cardinality: true = countable instances of this feature's shape, wrapped in an envelope `{count, combined?, default?, instances?}` — NOT a bare list (that is `array`). The envelope carries a `count`, an optional `combined` aggregate over all instances, a `default` applied to each, and optional per-instance `instances` joined by `ordinal`; a metered collection may state count/combined with no `instances` enumerated at all. Exclusive with `part` (a fixed named-parts map) and with `array` (a bare list)." }; readonly "schema": { readonly "type": "boolean" }; readonly "title": { readonly "en": "Repeated"; readonly "pt": "Repetido" } } }; readonly "title": { readonly "en": "Concept settings"; readonly "pt": "Configurações de conceito" } };

const _data: ConceptSettingsDataT = {
	"prop": {
		"compose": {
			...ComposeData,
			"description": {
				"en": "FLATTEN the named shapes' props into this def — a single slug or a list. The old `alias` (a same-shape rename) is just a one-element compose. Each target must be an object shape; a def groups props, never IS a scalar."
			},
			"schema": {
				"anyOf": [
					{
						"pattern": "^[a-z][a-z0-9_]*$",
						"type": "string"
					},
					{
						"items": {
							"pattern": "^[a-z][a-z0-9_]*$",
							"type": "string"
						},
						"minItems": 1,
						"type": "array"
					}
				]
			},
			"title": {
				"en": "Compose",
				"pt": "Compor"
			}
		},
		"count": {
			...CountData,
			"description": {
				"en": "Number of discrete instances of a repeated feature (1 is legal: one discrete circuit). Its PRESENCE is the explicit nature tag: present = discrete countable instance(s); absent = inherently-singular aggregate. Never count: 1 for an aggregate; never omit for something you could see two of."
			},
			"schema": {
				"minimum": 1,
				"type": "integer"
			},
			"title": {
				"en": "Count"
			}
		},
		"is_array": {
			...IsArrayData,
			"description": {
				"en": "The feature's intrinsic cardinality: true = the value IS a bare LIST of its shape, `[shape, shape, …]`, with nothing wrapping it (refs rows, register maps, per-string install facts). Every member is enumerated; there is no count or aggregate envelope — that is `repeated`. Plural slug = array, per the naming rule. Exclusive with `map` (a slug-keyed record) and with `repeated` (the counted-instance envelope)."
			},
			"schema": {
				"type": "boolean"
			},
			"title": {
				"en": "Is array",
				"pt": "É lista"
			}
		},
		"is_specification": {
			...IsSpecificationData,
			"description": {
				"en": "Flag: true marks a feature as a SPECIFICATION carrier — each quantity_kind field becomes a `specification` (identity + measurand + `intervals`, the one list of rated bands) — regions covering both the thing's own behaviour envelope) AND the sensor's readable span (a `rating: measurable` interval), told apart by axis, not split into separate slots. The first-class replacement for the quantity_kind→spec binding: a spec-carrying feature declares this instead of relying on its props happening to be quantity_kinds. Independent of cardinality (array/map/repeated): a specification feature may itself be a list/map of spec-carrying shapes."
			},
			"schema": {
				"type": "boolean"
			},
			"title": {
				"en": "Is specification",
				"pt": "É especificação"
			}
		},
		"map": {
			...MapData,
			"description": {
				"en": "The feature's intrinsic cardinality: true = the feature is a slug-keyed RECORD of its shape (vedirect_fields). Exclusive with array."
			},
			"schema": {
				"type": "boolean"
			},
			"title": {
				"en": "Map",
				"pt": "Mapa"
			}
		},
		"part": {
			...PartData,
			"description": {
				"en": "The def-language fixed-parts expansion: names a parts/<slug>.yaml map (e.g. electrical_ac_phase_three) whose kinds become the feature's {combined, default, part} breakdown. Exclusive with `repeated` (a counted instance set)."
			},
			"schema": {
				"pattern": "^[a-z][a-z0-9_]*$",
				"type": "string"
			},
			"title": {
				"en": "Part",
				"pt": "Parte"
			}
		},
		"repeated": {
			...RepeatedData,
			"description": {
				"en": "The feature's intrinsic cardinality: true = countable instances of this feature's shape, wrapped in an envelope `{count, combined?, default?, instances?}` — NOT a bare list (that is `array`). The envelope carries a `count`, an optional `combined` aggregate over all instances, a `default` applied to each, and optional per-instance `instances` joined by `ordinal`; a metered collection may state count/combined with no `instances` enumerated at all. Exclusive with `part` (a fixed named-parts map) and with `array` (a bare list)."
			},
			"schema": {
				"type": "boolean"
			},
			"title": {
				"en": "Repeated",
				"pt": "Repetido"
			}
		}
	},
	"title": {
		"en": "Concept settings",
		"pt": "Configurações de conceito"
	}
};
export default _data;
