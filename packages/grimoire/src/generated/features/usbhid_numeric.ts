// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import ByteOrderData, { ByteOrderSchema, type ByteOrder, type ByteOrderDataT } from '../property/byte_order.ts';
import ScaleOverridesData, { ScaleOverridesSchema, type ScaleOverrides, type ScaleOverridesDataT } from '../property/scale_overrides.ts';

export const UsbhidNumericSchema: TSchema = Type.Object({ "byte_order": Type.Optional(ByteOrderSchema), "scale_overrides": Type.Optional(ScaleOverridesSchema) }, {"additionalProperties":false});

export type UsbhidNumeric = { "byteOrder"?: ByteOrder; "scaleOverrides"?: ScaleOverrides };

export type UsbhidNumericDataT = { readonly "prop": { readonly "byte_order": Omit<ByteOrderDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Byte order for multi-byte types; omit ⇒ big_first (the network/HID default). Ignored for single-byte types." }; readonly "schema": { readonly "enum": readonly ["big_first", "little_first"]; readonly "type": "string" }; readonly "title": { readonly "en": "Byte order"; readonly "pt": "Ordem de bytes" } }; readonly "scale_overrides": Omit<ScaleOverridesDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Per-firmware-profile scale overrides, keyed by the profile a link firmware rule selects. When the resolved profile has an entry, it replaces `scale`; otherwise `scale` stands." }; readonly "schema": { readonly "additionalProperties": { readonly "type": "number" }; readonly "propertyNames": { readonly "pattern": "^[a-z][a-z0-9_]*$" }; readonly "type": "object" }; readonly "title": { readonly "en": "Scale overrides"; readonly "pt": "Escalas por firmware" } } } };

const _data: UsbhidNumericDataT = {
	"prop": {
		"byte_order": {
			...ByteOrderData,
			"description": {
				"en": "Byte order for multi-byte types; omit ⇒ big_first (the network/HID default). Ignored for single-byte types."
			},
			"schema": {
				"enum": [
					"big_first",
					"little_first"
				],
				"type": "string"
			},
			"title": {
				"en": "Byte order",
				"pt": "Ordem de bytes"
			}
		},
		"scale_overrides": {
			...ScaleOverridesData,
			"description": {
				"en": "Per-firmware-profile scale overrides, keyed by the profile a link firmware rule selects. When the resolved profile has an entry, it replaces `scale`; otherwise `scale` stands."
			},
			"schema": {
				"additionalProperties": {
					"type": "number"
				},
				"propertyNames": {
					"pattern": "^[a-z][a-z0-9_]*$"
				},
				"type": "object"
			},
			"title": {
				"en": "Scale overrides",
				"pt": "Escalas por firmware"
			}
		}
	}
};
export default _data;
