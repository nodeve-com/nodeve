// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import RegisterTypeData, { RegisterTypeSchema, type RegisterType, type RegisterTypeDataT } from '../property/register_type.ts';
import UnitIdData, { UnitIdSchema, type UnitId, type UnitIdDataT } from '../property/unit_id.ts';
import WordOrderData, { WordOrderSchema, type WordOrder, type WordOrderDataT } from '../property/word_order.ts';

export const ModbusLinkSchema: TSchema = Type.Object({ "register_type": Type.Optional(RegisterTypeSchema), "unit_id": Type.Optional(UnitIdSchema), "word_order": Type.Optional(WordOrderSchema) }, {"additionalProperties":false});

export type ModbusLink = { "registerType"?: RegisterType; "unitId"?: UnitId; "wordOrder"?: WordOrder };

export type ModbusLinkDataT = { readonly "prop": { readonly "register_type": Omit<RegisterTypeDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Which Modbus register space a read targets — holding = function 03, input = function 04. Composed by a device's link and a tap window." }; readonly "schema": { readonly "enum": readonly ["holding", "input"]; readonly "type": "string" }; readonly "title": { readonly "en": "Register type" } }; readonly "unit_id": Omit<UnitIdDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Modbus slave/unit id — addressing, the same on whichever wire reaches the device. A known per-unit default, overridable downstream (identical units multi-dropped on one bus each set their own)." }; readonly "schema": { readonly "maximum": 247; readonly "minimum": 0; readonly "type": "integer" }; readonly "title": { readonly "en": "Unit id" } }; readonly "word_order": Omit<WordOrderDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Word order for 32-bit values: high_first puts the high word at the lower address. 16-bit registers are always big-endian (Modbus standard)." }; readonly "schema": { readonly "enum": readonly ["high_first", "low_first"]; readonly "type": "string" }; readonly "title": { readonly "en": "Word order" } } } };

const _data: ModbusLinkDataT = {
	"prop": {
		"register_type": {
			...RegisterTypeData,
			"description": {
				"en": "Which Modbus register space a read targets — holding = function 03, input = function 04. Composed by a device's link and a tap window."
			},
			"schema": {
				"enum": [
					"holding",
					"input"
				],
				"type": "string"
			},
			"title": {
				"en": "Register type"
			}
		},
		"unit_id": {
			...UnitIdData,
			"description": {
				"en": "Modbus slave/unit id — addressing, the same on whichever wire reaches the device. A known per-unit default, overridable downstream (identical units multi-dropped on one bus each set their own)."
			},
			"schema": {
				"maximum": 247,
				"minimum": 0,
				"type": "integer"
			},
			"title": {
				"en": "Unit id"
			}
		},
		"word_order": {
			...WordOrderData,
			"description": {
				"en": "Word order for 32-bit values: high_first puts the high word at the lower address. 16-bit registers are always big-endian (Modbus standard)."
			},
			"schema": {
				"enum": [
					"high_first",
					"low_first"
				],
				"type": "string"
			},
			"title": {
				"en": "Word order"
			}
		}
	}
};
export default _data;
