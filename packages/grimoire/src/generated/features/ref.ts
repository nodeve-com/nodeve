// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import MatchData, { MatchSchema, type Match, type MatchDataT } from '../property/match.ts';
import RegistryIdData, { RegistryIdSchema, type RegistryId, type RegistryIdDataT } from '../property/registry_id.ts';
import TermData, { TermSchema, type Term, type TermDataT } from '../property/term.ts';

export const RefSchema: TSchema = Type.Object({ "registry_id": RegistryIdSchema, "term": TermSchema, "match": MatchSchema }, {"additionalProperties":false});

export type Ref = { "registryId": RegistryId; "term": Term; "match": Match };

export type RefDataT = { readonly "prop": { readonly "match": Omit<MatchDataT, "schema"> & { readonly "schema": { readonly "enum": readonly ["exact", "close", "broad", "narrow", "related"]; readonly "required": true } }; readonly "registry_id": Omit<RegistryIdDataT, "column" | "description" | "schema" | "title"> & { readonly "column": { readonly "references": "registry" }; readonly "description": { readonly "en": "Slug of the registry catalog entry whose id-space the crosswalk term is drawn from." }; readonly "schema": { readonly "pattern": "^[a-z][a-z0-9_]*$"; readonly "required": true; readonly "type": "string" }; readonly "title": { readonly "en": "Registry"; readonly "pt": "Registro" } }; readonly "term": Omit<TermDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "The identifier of an entry WITHIN a registry — the code that, resolved through the registry's IRI template, names the external concept a crosswalk ref points at (e.g. Temperature in qudt_quantity_kind, 2.3 in vim)." }; readonly "schema": { readonly "minLength": 1; readonly "required": true; readonly "type": "string" }; readonly "title": { readonly "en": "Term"; readonly "pt": "Termo" } } }; readonly "title": { readonly "en": "Crosswalk reference"; readonly "pt": "Referência de crosswalk" } };

const _data: RefDataT = {
	"prop": {
		"match": {
			...MatchData,
			"schema": {
				"enum": [
					"exact",
					"close",
					"broad",
					"narrow",
					"related"
				],
				"required": true
			}
		},
		"registry_id": {
			...RegistryIdData,
			"column": {
				"references": "registry"
			},
			"description": {
				"en": "Slug of the registry catalog entry whose id-space the crosswalk term is drawn from."
			},
			"schema": {
				"pattern": "^[a-z][a-z0-9_]*$",
				"required": true,
				"type": "string"
			},
			"title": {
				"en": "Registry",
				"pt": "Registro"
			}
		},
		"term": {
			...TermData,
			"description": {
				"en": "The identifier of an entry WITHIN a registry — the code that, resolved through the registry's IRI template, names the external concept a crosswalk ref points at (e.g. Temperature in qudt_quantity_kind, 2.3 in vim)."
			},
			"schema": {
				"minLength": 1,
				"required": true,
				"type": "string"
			},
			"title": {
				"en": "Term",
				"pt": "Termo"
			}
		}
	},
	"title": {
		"en": "Crosswalk reference",
		"pt": "Referência de crosswalk"
	}
};
export default _data;
