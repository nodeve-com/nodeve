// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';
import * as en_ from '../property/en.ts';
import * as i18N_ from './i18n.ts';
import * as pt_ from '../property/pt.ts';

export const schema: TSchema = Type.Object({ "en": Type.Optional(en_.schema), "pt": Type.Optional(pt_.schema) }, {"additionalProperties":false});

export type Body = { "en"?: en_.En; "pt"?: pt_.Pt };

type DataT = { readonly "prop": (typeof i18N_)["prop"]; readonly "description": { readonly "en": "Durable prose/rationale for a concept — the spec doc that survives every projection, distinct from the one-line display description. Author what a comment block would have carried." }; readonly "identity": { readonly "archetypeId": "feature"; readonly "slug": "body" }; readonly "title": { readonly "en": "Body" } };

const _data: DataT = {
	"prop": i18N_["prop"],
	"description": {
		"en": "Durable prose/rationale for a concept — the spec doc that survives every projection, distinct from the one-line display description. Author what a comment block would have carried."
	},
	"identity": {
		"archetypeId": "feature",
		"slug": "body"
	},
	"title": {
		"en": "Body"
	}
};
export const { description, identity, prop, title } = _data;
