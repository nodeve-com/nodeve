// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';
import * as enums_ from '../property/enums.ts';
import * as prop_ from '../property/prop.ts';

export const schema: TSchema = Type.Object({ "prop": Type.Optional(prop_.schema), "enums": Type.Optional(enums_.schema) }, {"additionalProperties":false});

export type FeatureSettings = { "prop"?: prop_.Prop; "enums"?: enums_.Enums };

type DataT = { readonly "body": { readonly "en": "GROUPING — the feature-level half of the def language, the mirror of features/archetype_settings.yaml. `prop` is the `<name>: overlay` map of fields the feature groups; `enums` binds enumerations, one field per named value set.\n\nWHY A SEPARATE BLOCK, not two more props on `concept_settings`: that block is composed by BOTH meta-defs, so a slot added there is legal on an archetype too — and a bare field or enum on a class is exactly what the layer model forbids (a property or an enum reaches an archetype only one layer down, INSIDE a feature). The three blocks split the def language by who may carry it: `concept_settings` shared, `feature_settings` feature-only, `archetype_settings` archetype-only. Each is declared by the meta-def that allows it, so \"feature-only\" and \"archetype-only\" hold BY CONSTRUCTION — the closed projection rejects an undeclared key, and nothing is stripped before it looks. That is what replaced a hand-authored allow-list (the deleted scripts/guard-archetype-features.ts) and, with it, the per-key exceptions that kept re-growing one.\n" }; readonly "description": { readonly "en": "The def-language grammar only a FEATURE may carry — the fields it groups and the enumerations it binds, kept out of the shared concept_settings block so an archetype can never reach them."; readonly "pt": "A gramática de def que só um ELEMENTO pode transportar — os campos que agrupa e as enumerações que associa, mantidos fora do bloco partilhado concept_settings para que um arquétipo nunca lhes chegue." }; readonly "identity": { readonly "archetypeId": "feature"; readonly "slug": "feature_settings" }; readonly "prop": { readonly "enums": typeof enums_; readonly "prop": typeof prop_ }; readonly "title": { readonly "en": "Feature settings"; readonly "pt": "Configurações de elemento" } };

const _data: DataT = {
	"body": {
		"en": "GROUPING — the feature-level half of the def language, the mirror of features/archetype_settings.yaml. `prop` is the `<name>: overlay` map of fields the feature groups; `enums` binds enumerations, one field per named value set.\n\nWHY A SEPARATE BLOCK, not two more props on `concept_settings`: that block is composed by BOTH meta-defs, so a slot added there is legal on an archetype too — and a bare field or enum on a class is exactly what the layer model forbids (a property or an enum reaches an archetype only one layer down, INSIDE a feature). The three blocks split the def language by who may carry it: `concept_settings` shared, `feature_settings` feature-only, `archetype_settings` archetype-only. Each is declared by the meta-def that allows it, so \"feature-only\" and \"archetype-only\" hold BY CONSTRUCTION — the closed projection rejects an undeclared key, and nothing is stripped before it looks. That is what replaced a hand-authored allow-list (the deleted scripts/guard-archetype-features.ts) and, with it, the per-key exceptions that kept re-growing one.\n"
	},
	"description": {
		"en": "The def-language grammar only a FEATURE may carry — the fields it groups and the enumerations it binds, kept out of the shared concept_settings block so an archetype can never reach them.",
		"pt": "A gramática de def que só um ELEMENTO pode transportar — os campos que agrupa e as enumerações que associa, mantidos fora do bloco partilhado concept_settings para que um arquétipo nunca lhes chegue."
	},
	"identity": {
		"archetypeId": "feature",
		"slug": "feature_settings"
	},
	"prop": {
		"enums": enums_,
		"prop": prop_
	},
	"title": {
		"en": "Feature settings",
		"pt": "Configurações de elemento"
	}
};
export const { body, description, identity, prop, title } = _data;
