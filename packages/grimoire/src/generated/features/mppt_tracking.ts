// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import MpptWindowData, { MpptWindowSchema, type MpptWindow, type MpptWindowDataT } from '../property/mppt_window.ts';

export const MpptTrackingSchema: TSchema = Type.Object({ "mppt_window": Type.Optional(MpptWindowSchema) }, {"additionalProperties":false});

export type MpptTracking = { "mpptWindow"?: MpptWindow };

export type MpptTrackingDataT = { readonly "prop": { readonly "mppt_window": Omit<MpptWindowDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "The MPPT tracking window, in volts (min = the floor where the converter parks a string once engaged but light is too low to track a real MPP; max = top of the hunt range). A property of the TRACKER, not of any single string — may be narrower than a string's rated voltage envelope. A bare range, not a voltage nominal-range: an MPP window has no rated point to nominate." }; readonly "schema": { readonly "type": "object" }; readonly "title": { readonly "en": "MPPT window" } } } };

const _data: MpptTrackingDataT = {
	"prop": {
		"mppt_window": {
			...MpptWindowData,
			"description": {
				"en": "The MPPT tracking window, in volts (min = the floor where the converter parks a string once engaged but light is too low to track a real MPP; max = top of the hunt range). A property of the TRACKER, not of any single string — may be narrower than a string's rated voltage envelope. A bare range, not a voltage nominal-range: an MPP window has no rated point to nominate."
			},
			"schema": {
				"type": "object"
			},
			"title": {
				"en": "MPPT window"
			}
		}
	}
};
export default _data;
