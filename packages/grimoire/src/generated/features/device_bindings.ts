// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import CatalogRefData, { CatalogRefSchema, type CatalogRef, type CatalogRefDataT } from '../property/catalog_ref.ts';
import FormatData, { FormatSchema, type Format, type FormatDataT } from '../property/format.ts';
import LinkData, { LinkSchema, type Link, type LinkDataT } from '../property/link.ts';
import PublishData, { PublishSchema, type Publish, type PublishDataT } from '../property/publish.ts';
import SlugData, { SlugSchema, type Slug, type SlugDataT } from '../property/slug.ts';
import TopicPrefixData, { TopicPrefixSchema, type TopicPrefix, type TopicPrefixDataT } from '../property/topic_prefix.ts';
import UnitIdData, { UnitIdSchema, type UnitId, type UnitIdDataT } from '../property/unit_id.ts';
import UpdateIntervalMsData, { UpdateIntervalMsSchema, type UpdateIntervalMs, type UpdateIntervalMsDataT } from '../property/update_interval_ms.ts';

export const DeviceBindingsSchema: TSchema = Type.Array(Type.Object({ "topic_prefix": Type.Optional(TopicPrefixSchema), "update_interval_ms": Type.Optional(UpdateIntervalMsSchema), "publish": Type.Optional(PublishSchema), "format": Type.Optional(FormatSchema), "slug": Type.Optional(SlugSchema), "link": Type.Optional(LinkSchema), "catalog_ref": Type.Optional(CatalogRefSchema), "unit_id": Type.Optional(UnitIdSchema) }, {"additionalProperties":false}));

export type DeviceBindings = Array<{ "topicPrefix"?: TopicPrefix; "updateIntervalMs"?: UpdateIntervalMs; "publish"?: Publish; "format"?: Format; "slug"?: Slug; "link"?: Link; "catalogRef"?: CatalogRef; "unitId"?: UnitId }>;

export type DeviceBindingsDataT = { readonly "array": { readonly "prop": { readonly "catalog_ref": Omit<CatalogRefDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Grimoire catalog reference (path or stable id) whose register map decodes this device. Omit to make the binding a raw passthrough — frames republished verbatim, no decode. Named catalog_ref (not profile) — it points at a catalog entry, distinct from usbhid_link's firmware-to-scale profile." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "Catalog reference" } }; readonly "format": Omit<FormatDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Wire layout for decoded points. grouped (default when absent): one retained JSON object per group at <topic_prefix>/<group> = {ts, mono, seq, ...values}. per_value: one retained bare-value topic per point at <topic_prefix>/<group>/<key> (no envelope) — the flat layout legacy per-value subscribers expect. Medium-neutral so any decoder-to-MQTT medium can offer either contract." }; readonly "schema": { readonly "enum": readonly ["grouped", "per_value"]; readonly "type": "string" }; readonly "title": { readonly "en": "Wire format" } }; readonly "link": Omit<LinkDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "The ingest link this device runs on, by its slug (the runtime resolves the reference). A slug value but a distinct key — it names a relationship, not this object's own id." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "Link" } }; readonly "publish": Omit<PublishDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "When to publish: on change only, or every sample." }; readonly "schema": { readonly "enum": readonly ["changed", "always"]; readonly "type": "string" }; readonly "title": { readonly "en": "Publish policy" } }; readonly "slug": Omit<SlugDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Machine identifier — the wire form of a code/key; names a whole thing on the bus. Never human text (that is i18n_text)."; readonly "pt": "Identificador de máquina — a forma de fio de um código/chave; nomeia uma coisa inteira no barramento. Nunca texto humano (isso é i18n_text)." }; readonly "schema": { readonly "pattern": "^[a-z][a-z0-9_]*$"; readonly "type": "string" }; readonly "title": { readonly "en": "Slug"; readonly "pt": "Slug" } }; readonly "topic_prefix": Omit<TopicPrefixDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "MQTT topic root a decoded value is published under." }; readonly "schema": { readonly "minLength": 1; readonly "type": "string" }; readonly "title": { readonly "en": "Topic prefix" } }; readonly "unit_id": Omit<UnitIdDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Modbus slave/unit id — addressing, the same on whichever wire reaches the device. A known per-unit default, overridable downstream (identical units multi-dropped on one bus each set their own)." }; readonly "schema": { readonly "maximum": 247; readonly "minimum": 0; readonly "type": "integer" }; readonly "title": { readonly "en": "Unit id" } }; readonly "update_interval_ms": Omit<UpdateIntervalMsDataT, "description" | "schema" | "title"> & { readonly "description": { readonly "en": "Cadence at which a fresh point is expected/emitted, integer milliseconds (industrial Modbus-master convention). Named for the RATE, not a fetch strategy — a polling driver reads at this interval, a push/discovered driver throttles to it. Required in the catalog emit contract; optional where a downstream consumer overrides the catalog default." }; readonly "schema": { readonly "exclusiveMinimum": 0; readonly "type": "integer" }; readonly "title": { readonly "en": "Update interval (ms)" } } } }; readonly "title": { readonly "en": "Device bindings"; readonly "pt": "Vinculações de dispositivos" } };

const _data: DeviceBindingsDataT = {
	"array": {
		"prop": {
			"catalog_ref": {
				...CatalogRefData,
				"description": {
					"en": "Grimoire catalog reference (path or stable id) whose register map decodes this device. Omit to make the binding a raw passthrough — frames republished verbatim, no decode. Named catalog_ref (not profile) — it points at a catalog entry, distinct from usbhid_link's firmware-to-scale profile."
				},
				"schema": {
					"minLength": 1,
					"type": "string"
				},
				"title": {
					"en": "Catalog reference"
				}
			},
			"format": {
				...FormatData,
				"description": {
					"en": "Wire layout for decoded points. grouped (default when absent): one retained JSON object per group at <topic_prefix>/<group> = {ts, mono, seq, ...values}. per_value: one retained bare-value topic per point at <topic_prefix>/<group>/<key> (no envelope) — the flat layout legacy per-value subscribers expect. Medium-neutral so any decoder-to-MQTT medium can offer either contract."
				},
				"schema": {
					"enum": [
						"grouped",
						"per_value"
					],
					"type": "string"
				},
				"title": {
					"en": "Wire format"
				}
			},
			"link": {
				...LinkData,
				"description": {
					"en": "The ingest link this device runs on, by its slug (the runtime resolves the reference). A slug value but a distinct key — it names a relationship, not this object's own id."
				},
				"schema": {
					"minLength": 1,
					"type": "string"
				},
				"title": {
					"en": "Link"
				}
			},
			"publish": {
				...PublishData,
				"description": {
					"en": "When to publish: on change only, or every sample."
				},
				"schema": {
					"enum": [
						"changed",
						"always"
					],
					"type": "string"
				},
				"title": {
					"en": "Publish policy"
				}
			},
			"slug": {
				...SlugData,
				"description": {
					"en": "Machine identifier — the wire form of a code/key; names a whole thing on the bus. Never human text (that is i18n_text).",
					"pt": "Identificador de máquina — a forma de fio de um código/chave; nomeia uma coisa inteira no barramento. Nunca texto humano (isso é i18n_text)."
				},
				"schema": {
					"pattern": "^[a-z][a-z0-9_]*$",
					"type": "string"
				},
				"title": {
					"en": "Slug",
					"pt": "Slug"
				}
			},
			"topic_prefix": {
				...TopicPrefixData,
				"description": {
					"en": "MQTT topic root a decoded value is published under."
				},
				"schema": {
					"minLength": 1,
					"type": "string"
				},
				"title": {
					"en": "Topic prefix"
				}
			},
			"unit_id": {
				...UnitIdData,
				"description": {
					"en": "Modbus slave/unit id — addressing, the same on whichever wire reaches the device. A known per-unit default, overridable downstream (identical units multi-dropped on one bus each set their own)."
				},
				"schema": {
					"maximum": 247,
					"minimum": 0,
					"type": "integer"
				},
				"title": {
					"en": "Unit id"
				}
			},
			"update_interval_ms": {
				...UpdateIntervalMsData,
				"description": {
					"en": "Cadence at which a fresh point is expected/emitted, integer milliseconds (industrial Modbus-master convention). Named for the RATE, not a fetch strategy — a polling driver reads at this interval, a push/discovered driver throttles to it. Required in the catalog emit contract; optional where a downstream consumer overrides the catalog default."
				},
				"schema": {
					"exclusiveMinimum": 0,
					"type": "integer"
				},
				"title": {
					"en": "Update interval (ms)"
				}
			}
		}
	},
	"title": {
		"en": "Device bindings",
		"pt": "Vinculações de dispositivos"
	}
};
export default _data;
