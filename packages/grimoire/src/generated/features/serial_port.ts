// GENERATED by `pnpm generate` from concepts/features/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';
import * as baudRate_ from '../property/baud_rate.ts';
import * as path_ from '../property/path.ts';

export const schema: TSchema = Type.Object({ "path": Type.Optional(path_.schema), "baudRate": Type.Optional(baudRate_.schema) }, {"additionalProperties":false,"x-key-map":{"baud_rate":"baudRate"}});

export type SerialPort = { "path"?: path_.Path; "baudRate"?: baudRate_.BaudRate };

type DataT = { readonly "identity": { readonly "archetype": "feature"; readonly "slug": "serial_port" }; readonly "prop": { readonly "baudRate": typeof baudRate_; readonly "path": Omit<typeof path_, "description" | "title"> & { readonly "description": { readonly "en": "OS serial device path, e.g. /dev/ttyUSB0 — a sensible default the consuming gateway may override / auto-discover (ttyUSB* renumbers across reboots/replugs)." }; readonly "title": { readonly "en": "Device" } } } };

const _data: DataT = {
	"identity": {
		"archetype": "feature",
		"slug": "serial_port"
	},
	"prop": {
		"baudRate": baudRate_,
		"path": {
			...path_,
			"description": {
				"en": "OS serial device path, e.g. /dev/ttyUSB0 — a sensible default the consuming gateway may override / auto-discover (ttyUSB* renumbers across reboots/replugs)."
			},
			"title": {
				"en": "Device"
			}
		}
	}
};
export const { identity, prop } = _data;
