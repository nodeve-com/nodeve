// GENERATED by `pnpm generate` — one catalog entry as the module default, composed into
// catalog/index.ts (camelCase keys — the snake wire shape is the <slug>.json twin). A repeated-
// feature spec the emit fills into every part (the authored `default`) is lifted to one `_sN`
// const and referenced per part. Do not edit by hand — edit the YAML, regenerate.

export default {
	"acPhase": {
		"featureSpec": {
			"combined": {
				"activePower": {
					"intervals": [
						{
							"identity": {
								"slug": "running"
							},
							"interval": {
								"intervalKind": "rating",
								"severity": "nominal",
								"tolerance": 33,
								"value": 38
							},
							"title": {
								"en": "Program running"
							}
						},
						{
							"identity": {
								"slug": "heating"
							},
							"interval": {
								"intervalKind": "rating",
								"severity": "nominal",
								"tolerance": 200,
								"value": 1500
							},
							"title": {
								"en": "Heating element engaged"
							}
						}
					]
				}
			}
		}
	},
	"body": {
		"en": "Generic dishwasher — a nominal `appliance` class stand-in (no real make/model). Its active-power draw is stated as rated `active_power` intervals on the `ac_phase` (the AC it draws from). The two draw regimes are the unit's own program states — each is an IDENTIFIED intervals band (its own identity.slug + title naming the state), so a control loop can reference the band by {feature, property, interval} rather than the old free-form `condition` gate. A deployment binds its own power sensor + per-band binary sensors and refits the envelopes from that unit's measured history (SITE data, in ha-config); these are the nominal class figures.\n"
	},
	"identity": {
		"archetypeId": "appliance",
		"code": "VF2TSKQD",
		"slug": "dishwasher"
	},
	"product": {
		"manufacturerId": "generic",
		"model": "dishwasher"
	}
} as const;
