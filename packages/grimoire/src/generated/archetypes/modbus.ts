// GENERATED by `pnpm generate` from concepts/archetypes/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';
import * as modbusConstraints_ from '../features/modbus_constraints.ts';
import * as modbusDecodes_ from '../features/modbus_decodes.ts';
import * as modbusLink_ from '../features/modbus_link.ts';
import * as modbusRegisters_ from '../features/modbus_registers.ts';
import * as serialPort_ from '../features/serial_port.ts';
import * as serialWire_ from '../features/serial_wire.ts';

export const schema: TSchema = Type.Object({ "serialPort": Type.Optional(serialPort_.schema), "serialWire": Type.Optional(serialWire_.schema), "modbusLink": Type.Optional(modbusLink_.schema), "modbusRegisters": Type.Optional(modbusRegisters_.schema), "modbusDecodes": Type.Optional(modbusDecodes_.schema), "modbusConstraints": Type.Optional(modbusConstraints_.schema) }, {"additionalProperties":false,"x-key-map":{"serial_port":"serialPort","serial_wire":"serialWire","modbus_link":"modbusLink","modbus_registers":"modbusRegisters","modbus_decodes":"modbusDecodes","modbus_constraints":"modbusConstraints"}});

export type Modbus = { "serialPort"?: serialPort_.SerialPort; "serialWire"?: serialWire_.SerialWire; "modbusLink"?: modbusLink_.ModbusLink; "modbusRegisters"?: modbusRegisters_.ModbusRegisters; "modbusDecodes"?: modbusDecodes_.ModbusDecodes; "modbusConstraints"?: modbusConstraints_.ModbusConstraints };

type DataT = { readonly "body": { readonly "en": "CONNECTIVITY MEDIUM, not a device: a composition assigning atoms to named slots. `transport` (the wire) and `emit` (the MQTT output) are SHARED with every other decoder→MQTT medium; the rest is modbus framing. Slot names are the medium's local property (`link`, not `modbus_link`) — atoms define contents, the archetype names the slot. IS a device's `connectivity` block, keyed by its `modbus` slot on the device archetype — that slot key IS the discriminator (no in-band field repeats it). Ships a standalone schema.json for non-TS gateways; no catalog instances of its own.\n\nOSI: Modbus is a true Layer-7 APPLICATION protocol, transport-independent (rides RS-485 serial or TCP — `transport` picks). The `refs` crosswalk records that identity. FUTURE (layer-honest model, docs/communication_protocol_standards.md): a lower-layer axis (interface_type async_serial, the transport already carries physical_layer) could be pinned here — Modbus RTU vs TCP framing lives in the transport, so add it there, not as a fake application-layer enum.\n" }; readonly "description": { readonly "en": "A Modbus connectivity medium — Layer-7 application protocol (transport + emit + register map + decode + read-batching constraints)." }; readonly "identity": { readonly "archetypeId": "archetype"; readonly "slug": "modbus" }; readonly "prop": { readonly "modbusConstraints": typeof modbusConstraints_; readonly "modbusDecodes": typeof modbusDecodes_; readonly "modbusLink": typeof modbusLink_; readonly "modbusRegisters": typeof modbusRegisters_; readonly "serialPort": typeof serialPort_; readonly "serialWire": typeof serialWire_ }; readonly "refs": readonly [{ readonly "match": "exact"; readonly "registryId": "modbus_org"; readonly "term": "Modbus Application Protocol" }]; readonly "title": { readonly "en": "Modbus medium" } };

const _data: DataT = {
	"body": {
		"en": "CONNECTIVITY MEDIUM, not a device: a composition assigning atoms to named slots. `transport` (the wire) and `emit` (the MQTT output) are SHARED with every other decoder→MQTT medium; the rest is modbus framing. Slot names are the medium's local property (`link`, not `modbus_link`) — atoms define contents, the archetype names the slot. IS a device's `connectivity` block, keyed by its `modbus` slot on the device archetype — that slot key IS the discriminator (no in-band field repeats it). Ships a standalone schema.json for non-TS gateways; no catalog instances of its own.\n\nOSI: Modbus is a true Layer-7 APPLICATION protocol, transport-independent (rides RS-485 serial or TCP — `transport` picks). The `refs` crosswalk records that identity. FUTURE (layer-honest model, docs/communication_protocol_standards.md): a lower-layer axis (interface_type async_serial, the transport already carries physical_layer) could be pinned here — Modbus RTU vs TCP framing lives in the transport, so add it there, not as a fake application-layer enum.\n"
	},
	"description": {
		"en": "A Modbus connectivity medium — Layer-7 application protocol (transport + emit + register map + decode + read-batching constraints)."
	},
	"identity": {
		"archetypeId": "archetype",
		"slug": "modbus"
	},
	"prop": {
		"modbusConstraints": modbusConstraints_,
		"modbusDecodes": modbusDecodes_,
		"modbusLink": modbusLink_,
		"modbusRegisters": modbusRegisters_,
		"serialPort": serialPort_,
		"serialWire": serialWire_
	},
	"refs": [
		{
			"match": "exact",
			"registryId": "modbus_org",
			"term": "Modbus Application Protocol"
		}
	],
	"title": {
		"en": "Modbus medium"
	}
};
export const { body, description, identity, prop, refs, title } = _data;
