// GENERATED by `pnpm generate` from concepts/archetypes/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The module IS the
// def node: the authored fields (labels/refs/ui) as named exports beside the live camelCase
// TypeBox `schema` (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases — `Value.Check(schema, …)` validates data renamed at the parse edge,
// src/parse.ts) and the parsed-instance type. Composed slots spread the sibling's namespace
// (fields + `schema`) so a shape lives once; snake never enters a .ts emit.

import { type TSchema, Type } from '@sinclair/typebox';
import * as body_ from '../features/body.ts';
import * as conceptSettings_ from '../features/concept_settings.ts';
import * as description_ from '../features/description.ts';
import * as featureSpec_ from '../features/feature_spec.ts';
import * as identity_ from '../features/identity.ts';
import * as refs_ from '../features/refs.ts';
import * as thing_ from './thing.ts';
import * as title_ from '../features/title.ts';

export const schema: TSchema = Type.Object({ "title": Type.Optional(title_.schema), "description": Type.Optional(description_.schema), "body": Type.Optional(body_.schema), "identity": Type.Optional(identity_.schema), "refs": Type.Optional(refs_.schema), "conceptSettings": Type.Optional(conceptSettings_.schema), "featureSpec": Type.Optional(featureSpec_.schema) }, {"additionalProperties":false,"x-key-map":{"concept_settings":"conceptSettings","feature_spec":"featureSpec"}});

export type Feature = { "title"?: title_.Title; "description"?: description_.Description; "body"?: body_.Body; "identity"?: identity_.Identity; "refs"?: refs_.Refs; "conceptSettings"?: conceptSettings_.ConceptSettings; "featureSpec"?: featureSpec_.FeatureSpec };

type DataT = { readonly "body": { readonly "en": "The FEATURE-OF-INTEREST — a named thing whose readings a value/gate/decode belongs to (pv_tracker, enclosure, grid, battery, ...). Two shapes (same split as quantity_kind): definition — a feature IS a Term (coded id + i18n label + refs); the features themselves are INSTANCES authored as data under property/feature/; a point's display label DERIVES from the referenced feature's label (+ ordinal + quantity_kind label), never stored per-link. A feature-of-interest is a sosa:FeatureOfInterest; grimoire owns the per-feature identities (no external registry enumerates `grid`). reference — the `feature` slug key, defined ONCE (property/identity/feature): measurand links, condition gates and categorical decode entries all compose it. Loose slug (open set).\n\nA feature IS a `thing` (title / description / identity — the unique slug) PLUS the two def-language slots below. `compose: [thing]` above flattens the thing fields in; these two are the feature's own grammar + spec body. DIRECTION (docs/feature-model.md): a feature def, not just a catalog instance, may carry a `feature_spec` — its agnostic default bands — gated by `concept_settings.is_specification`.\n\nThe REST of the definition-language shape (enums/props/prop maps a def may carry) is still the COMPILER's contract (kit/compile.ts) — deferred until the def language itself is schema'd.\n\nCROSS-FIELD CONSTRAINTS are NOT deferred and NOT a def-language job: author raw draft-07 keywords (if/then/else, oneOf, dependentRequired) in the feature's own `schema:` slot — kit/project.ts merges an object node's `schema:` into its emitted object schema (same passthrough properties use; it's a projection key in kit/compile.ts `instructionKeys`, so doc validation permits it). NEVER coin a concept_settings primitive for a rule JSON Schema already expresses (see inet_socket.yaml: transport==tcp ⇒ reconnect fields required).\n" }; readonly "description": { readonly "en": "A named thing whose readings a value/gate/decode belongs to (pv_tracker, enclosure, grid, battery, ...) — a sosa:FeatureOfInterest; also the layer's def shape (a grouping of props)."; readonly "pt": "Uma coisa nomeada a que pertencem leituras (pv_tracker, enclosure, grid, battery, ...) — um sosa:FeatureOfInterest; também a forma de def da camada (um agrupamento de props)." }; readonly "identity": { readonly "archetypeId": "archetype"; readonly "slug": "feature" }; readonly "prop": { readonly "body": typeof body_; readonly "conceptSettings": typeof conceptSettings_; readonly "description": typeof description_; readonly "featureSpec": typeof featureSpec_; readonly "identity": typeof identity_; readonly "refs": typeof refs_; readonly "title": typeof title_ }; readonly "refs": readonly [{ readonly "match": "close"; readonly "registryId": "sosa"; readonly "term": "FeatureOfInterest" }]; readonly "title": { readonly "en": "Feature of Interest"; readonly "pt": "Elemento de interesse" } };

const _data: DataT = {
	"body": {
		"en": "The FEATURE-OF-INTEREST — a named thing whose readings a value/gate/decode belongs to (pv_tracker, enclosure, grid, battery, ...). Two shapes (same split as quantity_kind): definition — a feature IS a Term (coded id + i18n label + refs); the features themselves are INSTANCES authored as data under property/feature/; a point's display label DERIVES from the referenced feature's label (+ ordinal + quantity_kind label), never stored per-link. A feature-of-interest is a sosa:FeatureOfInterest; grimoire owns the per-feature identities (no external registry enumerates `grid`). reference — the `feature` slug key, defined ONCE (property/identity/feature): measurand links, condition gates and categorical decode entries all compose it. Loose slug (open set).\n\nA feature IS a `thing` (title / description / identity — the unique slug) PLUS the two def-language slots below. `compose: [thing]` above flattens the thing fields in; these two are the feature's own grammar + spec body. DIRECTION (docs/feature-model.md): a feature def, not just a catalog instance, may carry a `feature_spec` — its agnostic default bands — gated by `concept_settings.is_specification`.\n\nThe REST of the definition-language shape (enums/props/prop maps a def may carry) is still the COMPILER's contract (kit/compile.ts) — deferred until the def language itself is schema'd.\n\nCROSS-FIELD CONSTRAINTS are NOT deferred and NOT a def-language job: author raw draft-07 keywords (if/then/else, oneOf, dependentRequired) in the feature's own `schema:` slot — kit/project.ts merges an object node's `schema:` into its emitted object schema (same passthrough properties use; it's a projection key in kit/compile.ts `instructionKeys`, so doc validation permits it). NEVER coin a concept_settings primitive for a rule JSON Schema already expresses (see inet_socket.yaml: transport==tcp ⇒ reconnect fields required).\n"
	},
	"description": {
		"en": "A named thing whose readings a value/gate/decode belongs to (pv_tracker, enclosure, grid, battery, ...) — a sosa:FeatureOfInterest; also the layer's def shape (a grouping of props).",
		"pt": "Uma coisa nomeada a que pertencem leituras (pv_tracker, enclosure, grid, battery, ...) — um sosa:FeatureOfInterest; também a forma de def da camada (um agrupamento de props)."
	},
	"identity": {
		"archetypeId": "archetype",
		"slug": "feature"
	},
	"prop": {
		"body": body_,
		"conceptSettings": conceptSettings_,
		"description": description_,
		"featureSpec": featureSpec_,
		"identity": identity_,
		"refs": refs_,
		"title": title_
	},
	"refs": [
		{
			"match": "close",
			"registryId": "sosa",
			"term": "FeatureOfInterest"
		}
	],
	"title": {
		"en": "Feature of Interest",
		"pt": "Elemento de interesse"
	}
};
export const { body, description, identity, prop, refs, title } = _data;
