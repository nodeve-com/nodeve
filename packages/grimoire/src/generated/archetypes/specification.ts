// GENERATED by `pnpm generate` from concepts/archetypes/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The snake_case
// TypeBox schema transcribed from the compiled draft-07 projection (its own <name>.schema.json);
// `Value.Check(<Name>Schema, …)` validates, the camelCase type is the surface consumers code
// against. The `export default` is the full DATA tree (labels/refs/ui) — the .ts twin of the
// sibling .json; composed slots spread their sibling `<Name>Data` default so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import IdentityData, { IdentitySchema, type Identity, type IdentityDataT } from '../features/identity.ts';
import IntervalsData, { IntervalsSchema, type Intervals, type IntervalsDataT } from './intervals.ts';
import MeasurandData, { MeasurandSchema, type Measurand, type MeasurandDataT } from '../features/measurand.ts';

export const SpecificationSchema: TSchema = Type.Object({ "identity": Type.Optional(IdentitySchema), "measurand": Type.Optional(MeasurandSchema), "intervals": Type.Optional(IntervalsSchema) }, {"additionalProperties":false});

export type Specification = { "identity"?: Identity; "measurand"?: Measurand; "intervals"?: Intervals };

export type SpecificationDataT = { readonly "description": { readonly "en": "The value of a quantity_kind field on an `is_specification` feature — its on-bus identity, measurand metrology, and the `intervals` list of rated/characterised bands." }; readonly "prop": { readonly "identity": IdentityDataT; readonly "intervals": Omit<IntervalsDataT, "description" | "title"> & { readonly "description": { readonly "en": "One identified rated interval of a quantity — bounds + gating conditions under an identity.slug handle. The item type of a specification's `intervals` list." }; readonly "title": { readonly "en": "Intervals" } }; readonly "measurand": Omit<MeasurandDataT, "description" | "title"> & { readonly "description": { readonly "en": "The metrology facts of a quantity kind — its unit, SI unit, and default accumulation. The kind's enum member carries the site-agnostic defaults; a `specification` composes measurand so a device/site can OVERRIDE them. The deterministic on-bus `slug` lives on the specification's `identity`, not here." }; readonly "title": { readonly "en": "Measurand" } } }; readonly "title": { readonly "en": "Specification" } };

const _data: SpecificationDataT = {
	"description": {
		"en": "The value of a quantity_kind field on an `is_specification` feature — its on-bus identity, measurand metrology, and the `intervals` list of rated/characterised bands."
	},
	"prop": {
		"identity": IdentityData,
		"intervals": {
			...IntervalsData,
			"description": {
				"en": "One identified rated interval of a quantity — bounds + gating conditions under an identity.slug handle. The item type of a specification's `intervals` list."
			},
			"title": {
				"en": "Intervals"
			}
		},
		"measurand": {
			...MeasurandData,
			"description": {
				"en": "The metrology facts of a quantity kind — its unit, SI unit, and default accumulation. The kind's enum member carries the site-agnostic defaults; a `specification` composes measurand so a device/site can OVERRIDE them. The deterministic on-bus `slug` lives on the specification's `identity`, not here."
			},
			"title": {
				"en": "Measurand"
			}
		}
	},
	"title": {
		"en": "Specification"
	}
};
export default _data;
