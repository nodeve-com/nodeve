// GENERATED by `pnpm generate` from concepts/archetypes/ via kit/compile.ts +
// kit/emit-types.ts. Do not edit by hand — edit the YAML and regenerate. The camelCase
// TypeBox schema (@nodeve/schema-case projection of the draft-07; `x-key-map` carries the
// snake aliases); `Value.Check(<Name>Schema, …)` validates data already renamed at the parse
// edge (src/parse.ts). The `export default` is the AUTHORED data tree (labels/refs/ui) — the
// sibling .json's content, camelCase keys (snake never enters a .ts emit); composed slots spread
// their sibling `<Name>Data` so a shape lives once.

import { type TSchema, Type } from '@sinclair/typebox';
import IdentityData, { IdentitySchema, type Identity, type IdentityDataT } from '../features/identity.ts';
import IntervalsData, { IntervalsSchema, type Intervals, type IntervalsDataT } from './intervals.ts';
import MeasurandData, { MeasurandSchema, type Measurand, type MeasurandDataT } from '../features/measurand.ts';

export const SpecificationSchema: TSchema = Type.Object({ "identity": Type.Optional(IdentitySchema), "measurand": Type.Optional(MeasurandSchema), "intervals": Type.Optional(IntervalsSchema) }, {"additionalProperties":false});

export type Specification = { "identity"?: Identity; "measurand"?: Measurand; "intervals"?: Intervals };

export type SpecificationDataT = { readonly "description": { readonly "en": "The value of a quantity_kind field on an `is_specification` feature — its on-bus identity, measurand metrology, and the `intervals` list of rated/characterised bands." }; readonly "prop": { readonly "identity": IdentityDataT; readonly "intervals": IntervalsDataT; readonly "measurand": MeasurandDataT }; readonly "title": { readonly "en": "Specification" } };

const _data: SpecificationDataT = {
	"description": {
		"en": "The value of a quantity_kind field on an `is_specification` feature — its on-bus identity, measurand metrology, and the `intervals` list of rated/characterised bands."
	},
	"prop": {
		"identity": IdentityData,
		"intervals": IntervalsData,
		"measurand": MeasurandData
	},
	"title": {
		"en": "Specification"
	}
};
export default _data;
