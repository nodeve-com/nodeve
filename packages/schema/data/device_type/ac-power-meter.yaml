# AC power meter — one AC point. The connection's phase count is the model's own
# (part_set: three-phase / split-phase); the type stays phase-agnostic.
socket_binding: { point: { feature_type: ac-phase, required: true } }
