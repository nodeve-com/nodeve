facet:
  product: { cardinality: single, required: true }
  feature_of_interest: { cardinality: child, required: true }
  register_map: { cardinality: single }
  network_interface: { cardinality: child }
  service_binding: { cardinality: child }
